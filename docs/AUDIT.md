# DigitalCampus — Cross-App Audit

Updated 2026-07-13 (supersedes the 2026-07-08 pass — every finding below was re-verified against current code; nothing here is carried over unchecked). Scope: `eduweb`, `management`, `instructor`, `student`, `finance`, `payment`, `library`, plus `support` (new since the last pass, wired into `urls.py` but never previously audited). `parent` remains excluded — still an unwired stub. Still no automated tests anywhere (every `tests.py` is the untouched Django stub) — every fix below needs manual QA. **See Phase 8 (2026-07-21) for the latest cross-app re-audit — a six-agent pass covering every commit through `80bdd21`, including the newly-discovered `melbac` app. Nothing in Phase 8 has been fixed yet; it is audit-only, per explicit request.**

Five commits landed since the last pass: end-of-session progression/carry-over (new feature), a refund-flow rewrite, and per-module permission gating for the finance and support portals. This pass verifies what those actually changed vs. what they claimed to change — the short version is **two of the three "gating" commits are template-level only**, and the new progression feature has several correctness gaps serious enough to matter before it's relied on.

## Scorecard

| App | Route | Verdict |
|---|---|---|
| eduweb | `/` (shared kernel) | Two guaranteed 500s from last pass are still live; refund/webhook plumbing is solid; settings fail open to `DEBUG=True` if `.env` is missing. |
| management | `/management/` | New progression feature is arithmetically sound but has real double-run, session-timing, and escalation-avoidance holes. Permission matrix still decorative. |
| instructor | `/instructor/` | Still the best-built app; the two previously-flagged bugs (exam-import crash, unvalidated grade score) are both still live. |
| student | `/student/` | Grade fragmentation is no longer just a cosmetic disagreement — it now silently corrupts the new progression feature's CGPA/pass computation. Exam endpoints still under-gated. |
| finance | `/finance/` | "Gated by per-module permission" commit only hid buttons in templates — no view enforces the matrix. |
| payment | `/payment/` | Refund flow is genuinely fixed and Stripe-ordered correctly, but doesn't revert application payment state, and its own permission gate is also template-only despite the commit message. |
| library | `/library/` | Unchanged since the last pass — still the cleanest app, still binary access control. |
| support | `/support/` (new, unaudited before) | Well-built ticket/helpdesk console, real IDOR protections on self-service replies. Same template-only permission gating as finance. |

## Cross-cutting themes

1. **"Gated by permission X" now means three different things, and two of them are false.** Commits 363585b (finance) and e128fed (support) both only touched templates — they hide buttons/links based on `StaffPermissionsMatrix`, but neither `finance/views.py` nor `support/views.py` reads the matrix at all. A finance user with `can_delete=False` can still POST directly to the delete URL and it succeeds. This is the same problem the original audit flagged in `management` (finding #4), now confirmed in two more apps by commits whose own messages claim otherwise. **This is the single most important fix to prioritize** — it's a false sense of security, which is worse than a documented gap.

2. **Grade fragmentation has gone from a UX inconsistency to a data-integrity bug.** `CourseGrade` is still fed only by `QuizAttempt`/`AssignmentSubmission` (`student/views.py:_record_academic_grade`), never by `StudentExamResponse` (the CBT exam engine). The new progression feature computes CGPA and pass/fail entirely from `CourseGrade` (`management/progression.py`). Net effect: **a student who passed a course via the exam engine has no `CourseGrade` row, so they read as failed, and can be stuck in `repeated` status indefinitely with no course of action except an admin manually inserting a grade row.** This must be fixed before progression is trusted for real decisions.

3. **The new progression feature has no double-run protection, no session-timing check, and a real escalation-avoidance loophole.** No idempotency guard means re-submitting the confirm POST (double-click, back-button resubmit) can advance the same student twice. Progression can be run against a session that hasn't closed. A student can indefinitely dodge probation/withdrawal by simply not re-registering for a failed core course (`failed_courses` only counts *current-session* registrations). None of this is exploited maliciously today, but all three are one careless admin click away from a real transcript error.

4. **Two guaranteed 500s from the last audit are still exactly where they were.** `get_student_fee_summary` (`eduweb/views.py:1827-1829,1840`) still calls `.select_related()` on two `@property` methods and still references a settings key that doesn't exist. Zero-line-of-code fix, still not done.

5. **Role-gating is still copy-pasted, not shared**, now in five places instead of three: `management` (`is_admin`/`_is_admin`/`_is_staff`), `finance` (`is_finance_manager`), `payment` (`is_finance_manager` again, separately defined), `support` (`support_required`/`support_admin_required`), none importing `eduweb.decorators`.

6. **Settings fail open, not closed.** `DigitalCampus/settings.py` defaults `SECRET_KEY` to the literal string `"django-insecure-change-this-in-production"` and `DEBUG` to `True` when `.env` is absent or misread. A missing/misconfigured `.env` on a real deploy doesn't crash loudly — it silently serves stack traces with a known secret key. This is new since the last pass wasn't looking at settings.py specifically.

---

## Remediation plan (tracked)

Work is sequenced app-by-app, not by severity alone — **`student` goes first and is fully closed out before moving to the next app**, since it's the surface students touch daily and it's what the new progression feature depends on. Checkboxes are updated in place as work lands; this section is the live source of truth for "what's actually done," the sections below it stay as point-in-time findings.

Three product decisions were made on 2026-07-13 and are now binding on the fixes below:
- **Grade computation**: a course's `CourseGrade` is a weighted blend — **Exam 70% / Quiz 15% / Assignment 15%** by default, overridable per course, with any missing component's weight redistributed proportionally across whatever assessment types the course actually has (e.g., an exam-only course is 100% exam).
- **Fee gating**: course/LMS enrollment itself stays free by design — the payment gate belongs at portal-access/admission, before a student ever reaches the dashboard. This pass verifies that gate is real (the original audit found nothing currently blocks portal access on an unpaid balance) rather than assuming it.
- **Prerequisites**: hard block at registration — a student cannot register for a course whose prerequisite they haven't passed (`CourseGrade.is_passed=True`).

### Phase 1 — `student` (complete, 2026-07-13)

**1. Unify the three grade sources into one real `CourseGrade`**
- [x] Added per-course assessment weight fields (`exam_weight_pct`, `quiz_weight_pct`, `assignment_weight_pct`, default 70/15/15) to `Course` + migration `0040_course_assessment_weights`.
- [x] Added `CourseGrade.recompute_for_student_course(student, course, session, term='')` (`eduweb/models.py`) — weighted blend across `StudentExamResponse` (end-of-semester, graded), `QuizAttempt`, and `AssignmentSubmission`, re-normalizing weights across whichever components exist. `_record_academic_grade` in `student/views.py` is now a thin wrapper around it.
- [x] Wired the recompute into all three trigger points: lesson completion (`_record_academic_grade`), student-side auto-grading (`submit_exam`), and instructor manual grading (`instructor/views.py exam_grade_response`) — each only fires once a response is fully `GRADED`, wrapped in try/except with logging.
- [x] "Grades & Performance" now queries real `CourseGrade` rows instead of the commented-out query.
- [x] "Academic Records" now reads the same `CourseGrade` rows instead of its separate `StudentExamResponse`-only `ExamGradeProxy`.
- [x] Fixed a latent bug surfaced during verification: the assignment-percentage annotation (`F('score') * 100.0 / F('assignment__max_score')`) needs an explicit `ExpressionWrapper(..., output_field=FloatField())` — Django can't infer the output type across Decimal/float otherwise. This bug existed in the original `_record_academic_grade` too (any course with a graded assignment would have crashed it); fixed as part of this rewrite.
- Backfilling pre-existing `CourseGrade` rows created under the old exam-blind logic was not done — they'll self-correct the next time any trigger point fires for that student/course.

**2. Close the exam-endpoint authorization gap**
- [x] `exam_instructions`, `start_exam`, `get_exam_data` now carry `@student_required` and scope the `Exam` queryset to the requester's own active/completed enrollment, matching the pattern already used by `exam_list`. Verified via test client: a non-enrolled user gets 404, an enrolled user proceeds normally.

**3. Registration correctness**
- [x] `my_courses` now excludes courses the student already has a passing `CourseGrade` for from both the semester listing and the carry-over listing.
- [x] `register_semester_course`'s `CourseRegistration.objects.get_or_create(...)` is now wrapped in `try/except ValidationError`, turning the prerequisite-violation crash into a friendly `messages.error` + redirect. `CourseRegistration.clean()`'s existing prerequisite check (unchanged) now actually surfaces to the user.

**4. Payment gate**
- [x] `can_access_student_portal()` now also requires `self.is_paid` (application-fee clearance). No backfill/grandfather clause — applies immediately to all students, including previously-approved ones, per product decision. Verified via test client: flipping `payment_status` to `pending` correctly redirects an otherwise-approved student to `application_status`; reverted after the test.
- [x] `student_required`'s message now distinguishes "please pay your application fee" from the generic "still being processed" case.

**5. Performance**
- [x] `course_detail`'s per-lesson N+1 replaced with a single up-front `LessonProgress` query building a `set` of completed lesson IDs.
- [x] `progress`'s 28-day activity heatmap replaced the 84-query per-day loop with 3 `Counter`-bucketed queries covering the whole window.

**Verification performed**: `python manage.py check` and `makemigrations --check` both clean; `CourseGrade.recompute_for_student_course` exercised directly against real enrollment data and confirmed compatible with `management.progression.compute_cgpa`; `/student/grades/`, `/student/academic-records/`, `/student/courses/`, `/student/progress/` all return 200 via Django test client; exam-endpoint gating and the payment gate both verified to behave correctly for both allowed and blocked cases.

**Phase 1 addendum (2026-07-14 follow-up)** — the user's actual live complaint ("passed 100L, entered a new session, can't register a new-level course") turned out to be a *different, deeper* bug than anything above: `register_semester_course`/`register_all_semester_courses`/the dashboard's `registered_credit_total` scoped the student's credit-cap check across the wrong set of courses once a student had carry-overs spanning levels. Root-caused via reproduction against real dev-DB data (not static reading — an earlier hypothesis about `AcademicSession.is_registration_open`'s window was wrong and the user correctly pushed back on it). Fixed with a new `_registrable_course_ids(profile)` helper used consistently by all three call sites, plus:
- [x] `course_catalog` was excluding a student's own already-enrolled courses once their level/session moved on (`Q(id__in=enrolled_course_ids)` added) — this is what made the catalog look "empty" after progressing.
- [x] `grades.html` never actually rendered the `CourseGrade` data the view had been computing since the original Phase-1 fix — added the real "Official Course Grades" section and fixed a bogus `{% widthratio %}` "Avg. Grade" stat to show real CGPA.
- [x] `academic_records.html` — added the "Progression History" section (`CourseCarryOver`/`ProgressionDecisionLog` were computed but never surfaced).
- [x] `quiz_submit` (`student/views.py`) — found it never called `CourseGrade.recompute_for_student_course` at all (a gap in the original Phase-1 wiring); also rewrote to scope answer lookup to question (security fix), wrap in `transaction.atomic()` + `select_for_update()`, and handle `short_answer`/`essay` text responses with `needs_grading=True` (previously these question types had no rendering or submission path at all — `quiz_take.html`/`quiz_result.html` updated to match).
- [x] `save_answer` now rejects edits after `exam.end_datetime` (previously allowed answer changes after the exam window closed).
- [x] `mark_lesson_complete` crash for preview lessons with no enrollment; `course_detail`'s `Http404`-swallowing bug; `assignment.overdue_status` (was calling a broken `_is_overdue_override`).
- **Verified**: reproduced the original registration-blocked scenario end-to-end via test client against real data (100L → 200L with a first-semester carry-over) and confirmed a new second-level course now registers successfully; `/student/grades/` confirmed to render real CGPA and per-course grades in-browser-equivalent (test client HTML assertion), not just return 200.

### Phase 2 — instructor (complete, 2026-07-14)

**1. Notification/email plumbing**
- [x] Found `instructor_counts` context processor (`eduweb/context.py`) was fully implemented but never registered in `DigitalCampus/settings.py`'s `TEMPLATES[0]['OPTIONS']['context_processors']` — the instructor notification bell had been silently dead. Registered it; added `instructor_pending_exam_count` (submitted-but-ungraded `StudentExamResponse` count) to it.
- [x] `enroll_student`'s "send welcome email" checkbox was a no-op `# TODO` — implemented `send_course_enrollment_email` (`eduweb/emailservices.py`, matches the existing `EmailMultiAlternatives` HTML+text pattern) and wired it in. Verified via `override_settings(EMAIL_BACKEND=locmem)` + `mail.outbox`.

**2. Grading pipeline unification**
- [x] Added `CourseGrade.recompute_for_student_course` calls to `grade_submission` (assignment grading) and `quiz_attempt_detail`'s manual-grading POST branch — neither triggered a recompute before, so instructor-graded work could silently disagree with the dashboard/Grades/Academic Records/progression figures fed by the same model.
- [x] Built the full short-answer/essay quiz grading loop end-to-end: student submission (needs_grading flag, see Phase 1 addendum) → instructor grading UI in `quiz_attempt_detail.html`/`quiz_results.html` (status badges, grading form) → `CourseGrade` recompute → student notification (`_notify_instructor`) on completion. Same student-notification-on-grade-complete pattern added to `exam_grade_response`.

**3. Backend correctness/security**
- [x] `create_assessment`'s inline exam-question-file-import call now shares the same try/except (`_parse_exam_questions_from_file` helper, deduplicated from copy-pasted `ExamQuestion.objects.create()` blocks that referenced already-removed model fields) that the standalone `exam_import_questions` view already had — uploading a bad file while creating a new exam no longer 500s. *(Verified fixed in code this session — no longer matches the AUDIT.md finding below.)*
- [x] `grade_submission`'s score is now validated (`Decimal` parse + `0 <= score <= assignment.max_score` range check with a friendly re-render on failure) instead of writing `request.POST.get('score')` straight to `.save()`. *(Verified fixed in code this session.)*
- [x] Added `@require_POST` to `section_delete`/`lesson_delete` (were GET-triggerable deletes) plus dependent-data guards: `lesson_delete` blocks if `LessonProgress`/`QuizAttempt`/`AssignmentSubmission` exist; `delete_question` blocks if any `QuizResponse` references it; `delete_answer` blocks if any `QuizResponse.selected_answer` references it.
- [x] `CourseForm.clean_academic_course` added — previously any POST could relink an existing LMS course's `academic_course` FK after creation (mass-assignment/IDOR-adjacent).
- [x] Fixed `course_statistics`'s ORM JOIN-multiplication bug — combining `Count('enrollments')` and `Avg('rating')` (over a *different* reverse relation, `Review`) in one `.annotate()` inflates both aggregates via the cross-join. Split into `Count(..., distinct=True)` plus a separate `Review.objects.values_list('course_id').annotate(avg=Avg('rating'))` merged in Python.
- [x] `exam_update` now catches `ValidationError` specifically and surfaces `e.message_dict` (was a generic `except Exception`, swallowing the real validation message).
- [x] Cleanup: moved `from datetime import datetime` out of an inline import inside `enroll_student` to the top-level import.

**Still open (re-verified, unchanged from the 2026-07-13 pass — not touched this session)**
- `course_create`/`course_delete` still redirect to "contact an admin" / raise `PermissionDenied` unconditionally.

### Phase 3 — management (backend punch list closed, 2026-07-14)

**Templates/navigation — complete**, verified via Django test Client against real dev DB (create→edit→delete round trips, full page sweep, no regressions on student/instructor which share the same `base.html`):
- [x] All 9 admin-sidebar bugs fixed in `templates/management/base.html` (Acads Structure toggle missing `progression`/`categories`, "All Users" always-true active-state, Course Intakes/Payment Gateways/System Config links commented out of the nav, Course Categories had zero nav presence, Roles & Permissions never highlighted, Audit Log detail page didn't highlight its parent, Blog post/category edit pages didn't highlight their submenu).
- [x] `intakes_list` crash fixed — `CourseIntake.accepted_count`/`remaining_slots`/`is_full` referenced `self.applications`, but `CourseApplication` had no FK to `CourseIntake`. Added `CourseApplication.intake` FK (migration `0043_courseapplication_intake`).
- [x] Bonus bug found while making Course Categories reachable: its view/template referenced `CourseCategory.lms_courses`, but `LMSCourse.category` is a commented-out FK in `eduweb/models.py` — the page was a guaranteed 500 the moment it became reachable (likely why it had been hidden from the sidebar). Removed the dead `lms_courses` reference; **flagged, not built**: real course-categorization would need a live `LMSCourse.category` FK + UI, currently out of scope pending a product decision.
- [x] Built the entire missing course-category create/edit/delete (views + URLs + new `course_category_form.html` template) since the list page already linked to these unbuilt routes — delete guarded against dependent subcategories per the standing "no deletes with dependent data" rule. Deleted ~125 lines of dead, stale, previously-commented-out duplicate view code that referenced non-existent templates.
- [x] Closes the AUDIT.md finding below re: `role_assign` being cosmetic-only — `QuickRoleChangeForm.role` (the form backing `user_change_role`, the actual mutating endpoint) now excludes `'student'` server-side too, not just the picker dropdown. Verified: raw POST with `role=student` now 400s and the target's role is unchanged.

**Backend — done**, all verified via Django test Client against real dev DB (including the one real non-superuser admin account, `femiadmin`, not just superuser bypass):
- [x] Payment-gateway secrets now matrix-enforced: `payment_gateway_create/edit/delete` gated on `permissions.finance.can_create/edit/delete` (matches the template), delete also blocked if any `Transaction` references the gateway.
- [x] `staff_payroll_create` now checks `finance_payroll.can_create` (its edit/delete siblings already did).
- [x] Payroll template/server module-key mismatch fixed — `payroll.html` now checks `permissions.finance_payroll.can_*` (was `permissions.finance.can_*`).
- [x] **Found a real corollary bug while fixing the above**: the `'admin'` role had no `finance_payroll` row in `StaffPermissionsMatrix` at all (neither in the DB nor in the `ROLE_DEFAULT_PERMISSIONS` dict) — meaning the one non-superuser admin account in the dev DB was *already* silently blocked from payroll edit/delete before this session touched anything, since those checks pre-existed. Backfilled via migration `0044_seed_admin_finance_payroll_defaults`.
- [x] Role/permission changes now write `AuditLog(action='permission_change')` + notify the affected user, in both `user_change_role` and `user_permissions` POST (each wrapped in `transaction.atomic()`). `security_dashboard`'s existing query for these events will now show real data.
- [x] `broadcast_send` double-submit race fixed: added a `'sending'` status (migration `0045_broadcastmessage_sending_status`); the status check-and-flip now happens atomically (`transaction.atomic()` + `select_for_update()`) *before* the background thread starts. Also fixed the all-or-nothing failure — per-batch send exceptions are now caught individually so one bad address no longer discards an otherwise-successful send.
- [x] `announcement_create`: system/course fan-out converted to `Notification.objects.bulk_create(...)` (was one `_notify()` call per recipient). **Also found and fixed a real crash**: category-type announcements 500'd with `FieldError` — `enrollments__course__category` doesn't exist because `LMSCourse.category` is a commented-out FK (same root cause as the course_categories_list bug above). Now degrades to a `messages.warning` instead of a 500.
- [x] Admin inbox/notification views fixed — **not with `is_admin`**. Cross-referencing the sidebar template and role defaults showed `admin_inbox`/`notifications_view` are shared by *all* back-office staff (support/finance/admin), not admin-only — the navbar routes only students/instructors elsewhere. Added a new `is_staff_member()` predicate scoped to `{admin, support, finance}` instead; verified all roles land where they should.
- [x] N+1 in `applications_list` fixed (`'program'` added to `select_related`).
- [x] `enrollment_delete`/`certificate_delete`/`badge_delete`/`review_delete`/`student_badge_delete`/`intake_delete` now all have permission checks; `enrollment_delete`/`badge_delete`/`intake_delete` also got dependent-record guards (confirmed `Badge→StudentBadge` and `Enrollment→LessonProgress` are both `on_delete=CASCADE`, so these guards prevent real silent data loss, not just decorative ones — `Certificate`/`Review` are leaf records with nothing to guard against).
- [x] `transaction.atomic()` added around `admin_exam_approve/reject/publish` and `academic_session_set_current`'s multi-session loop (+ `select_for_update()`, + cleaned up a stray `__import__('datetime')` inline import).
- [x] **Role cleanup (user directive, 2026-07-14): only `student`/`instructor`/`admin`/`support`/`finance` are real roles; `content_manager`/`qa` purged from all live (uncommented) code** — `StaffPayroll.staff`'s `limit_choices_to` (migration `0046_alter_staffpayroll_staff`), `security_middleware.STAFF_ROLES`, `management/views.py`'s new `is_staff_member`/`staff_payroll_list`'s local role lists, and the dev-only `seed.py` management command (was generating `content_manager`/`qa` test users). Confirmed 0 existing users have either phantom role, so no data migration was needed.

**Transcript generation — done, 2026-07-14 (fourth pass)**. `issue_transcript` was a **guaranteed crash on every single call** — it wrote `application.transcript_issued_by = request.user` then `save(update_fields=[..., 'transcript_issued_by'])`, but `CourseApplication` had no such field at all (`ValueError`, caught by the view's own broad `except Exception`, which is why it always failed with a generic "Failed to issue transcript" message and never actually issued anything). Also found: the page's own copy claimed "Once requested, the transcript content is locked. Any pending results added after your request will not appear" — but no locking mechanism existed; the transcript re-rendered live `CourseGrade` data on every page load, so that claim was false. And `issue_transcript` had **zero UI entry point anywhere** — fully orphaned, reachable only by hand-crafting a POST.
- [x] Added `CourseApplication.transcript_issued_by` FK (fixes the crash) and `transcript_snapshot` JSONField (`encoder=DjangoJSONEncoder`) to actually lock content — migration `0047_courseapplication_transcript_fields`.
- [x] Added `CourseGrade.build_transcript_snapshot(student)` classmethod (`eduweb/models.py`) — the canonical, shared computation for both live rendering and the frozen snapshot, so `student` and `management` never hand-roll two copies of the same GPA/grade-grouping logic. Careful to cast Decimal→float before it goes anywhere near the JSON field, since a raw Decimal silently becomes a string after a DB round-trip (JSONField has no Decimal type) — would have made a *locked* score's `{% if g.score %}` truthiness disagree with a *live* score's for the 0.00 edge case.
- [x] `student/views.py academic_records`: requesting a transcript now calls `build_transcript_snapshot` and freezes it onto `CourseApplication.transcript_snapshot`; the view renders the frozen snapshot once `transcript_requested=True`, live data otherwise. Added a "Locked Transcript" badge + "Locked as of ..." timestamp so the UI's own claim is now true instead of aspirational.
- [x] `issue_transcript` now requires `transcript_requested=True` before it can be issued (was previously issuable — and would 500 — on any approved application whether requested or not), added a `permissions.applications.can_edit` matrix check (was `is_admin`-only), and builds the snapshot as a defensive fallback if somehow missing.
- [x] Built the missing UI: a "Transcript" section on `application_detail.html` (not requested / requested-and-pending with an "Issue Transcript" button / issued, mirroring the existing "Admission Acceptance Tracking" section's visual pattern) — this endpoint had never been reachable from anywhere before.
- **Verified end-to-end**: requested a transcript, added a *new* grade afterwards, confirmed the rendered page still showed the pre-request grade count only (proving the lock holds against live DB changes); issued via the new admin UI; confirmed issuing before a request is blocked and re-issuing an already-issued one is a no-op.

**`StaffPermissionsMatrix` create/edit enforcement — done, 2026-07-14 (fourth pass)**, all verified against both superuser and the real non-superuser `femiadmin` account:
- [x] Added `_has_permission` checks to: `faculty_create/edit`, `department_create/edit`, `program_create/edit`, `courses_list`'s inline create/edit actions (the *live* course CRUD — see below), `intake_create/edit`, `blog_post_create/edit/delete`, `blog_category_create/edit/delete` (delete also gained a dependent-post-count guard), `required_payment_create/edit/delete` (delete also guarded — `FeePayment.fee` is `on_delete=CASCADE`, so an unguarded delete would have silently wiped real payment history), `library_item_create/edit/delete`, `admin_exam_timetable_update`, `admin_question_moderation`.
- [x] **Found and deleted a second block of dead orphaned code** while doing this, same pattern as the earlier course-categories cleanup: `courses`/`course_create`/`course_detail`/`course_edit`/`course_delete` (management/views.py) were all fully commented out of `urls.py` and completely unreachable — the *real*, live course CRUD is inline inside `courses_list` (create/edit/delete via `action=` POST params to the same URL). Deleted ~155 dead lines and the matching commented URL block; added the missing permission checks to the actual live `create`/`edit` actions instead (delete already had one).
- [x] `LMSCourse.category` FK is still commented out in the model — flagged again, not built (see the two bugs it already caused, course_categories_list and announcement_create, both worked around this session). No product decision made on it yet; still the one open item from this whole punch list.

### Phase 4 — finance + support (2026-07-14)

**Sidebar / page-title glitches — complete.** Every template in `templates/finance/` and `support/templates/support/` extended `management/base.html` but none overrode `{% block breadcrumb %}` — the header always showed "Dashboard" regardless of the actual page. Added correct `breadcrumb`/`breadcrumb_title` blocks to all of them. Also fixed real sidebar active-state bugs in `templates/management/base.html`'s finance section: the Payments submenu's expand/highlight and the Required Payments link both checked `'payments' in request.path` / `'required-payments' in request.path`, but the `payment` app's real prefix is `/payment/` (singular) and its required-payments path has no hyphen — both checks were always false. Switched to `request.resolver_match.app_name`/`url_name` checks. Also removed a dead nested `include('payment.urls', namespace='payment')` in `finance/urls.py` under `/finance/payments/` — nothing ever reverses that namespace, every template link uses the real `payments:` registration at `/payment/`.

**`StaffPermissionsMatrix` enforcement — closed for finance, payment, and support**, mirroring `management`'s `_has_permission(request, module, action)` pattern (each app keeps its own copy — consolidating the five hand-rolled role-gating decorators into `eduweb.decorators` is still the one open "bigger build" below, unchanged this session):
- [x] `finance/views.py`: `subscription_list` (`finance_subscriptions.can_view`), `payroll_management` (`finance_payroll.can_view`/`can_create`), `payroll_detail` (`can_view`/`can_edit`), `payroll_delete`/`payroll_attachment_delete` (`can_delete`) — previously gated only by `is_finance_manager` (role check), matrix flags only hid template buttons.
- [x] `payment/views.py`: `refund_payment` now checks `finance_payments.can_edit` (matches `payment_detail.html`'s existing button gate, which the view never enforced); `required_payments_list`/`create`/`update`/`toggle`/`delete` (new — see below) check `finance_payments.can_view/create/edit/delete`.
- [x] `support/views.py`: `ticket_create` and every `ticket_detail` POST action (`can_create` for replies, `can_edit` for status/assign/escalate/priority/reopen); `kb_article_create/edit`, `faq_create`; `canned_create`, `announcement_create`; `sla_save`, `department_save/toggle_active`, `agent_toggle_available` (kept `support_admin_required` on these last three — that's a deliberate hard admin lock in the sidebar template, not a matrix bypass — but added matrix checks as defense-in-depth on top of it). Also switched `faq_delete`/`canned_delete`/`announcement_delete` from `support_admin_required` to `support_required` + an explicit `can_delete` check, since their templates (`faq_list.html`/`canned_list.html`/`announcement_list.html`) gate the delete button on the matrix flag, not on admin role — the view was stricter than what the UI promised.

**Real correctness/validation bugs found and fixed while wiring the above** (this is the "ensure pages are dynamic" part of the ask — several finance/payment pages were rendering static or silently-empty data):
- [x] **`payment_management`/`payment_detail` were badly out of sync with their own templates and have been rebuilt to match.** `templates/finance/payment_management.html` expects two paginated tabs (`app_page`/`fee_page`, GET params of the same name) plus `success_count`/`pending_count`/`refunded_count`/`total_app_payments`/`total_fee_payments`/`currency_symbol`; the view only ever provided `payments`/`completed_payments`/`pending_payments`/`failed_payments` — so the entire payments table silently rendered **zero rows on every load**, for every finance user, regardless of how many payments existed (Django template loops over an undefined context variable are just empty, not an error). Similarly `payment_detail.html` has fully-built `payment_type == 'application' / 'fee'` branching (with explicit `{# ✅ ... #}` comments noting fields shared across both models) but the view never set `payment_type` and only ever queried `ApplicationPayment` — so every application payment's badge was mislabeled "Fee Payment", and any `FeePayment` reference (e.g. from the fee tab once it renders) 404'd. Rebuilt `payment_management` to paginate both `ApplicationPayment` and `FeePayment` querysets with matching filters, and `payment_detail` to look up either model by reference and set `payment_type` correctly. Verified end-to-end against real dev DB: all payment rows now render, fee-payment detail pages render with the correct badge and no refund button (refunds remain application-fee-only, matching `refund_payment`'s Stripe-only implementation).
- [x] Two `{% url 'finance:payment_detail' ... %}` typos (wrong namespace — `payment_detail` lives under `payments:`, not `finance:`) in `payment_management.html`, and one identical typo in `invoice_generation.html` (`finance:payment_management` → `payments:payment_management`) — each was a guaranteed `NoReverseMatch` 500 the moment that link was actually rendered (the payment_management ones were masked by the empty-table bug above; fixing that bug is what surfaced these).
- [x] **`payment/urls.py` had a URL-ordering bug**: the single-segment catch-all `<str:payment_reference>/` (feeding `payment_detail`) was registered second, before the literal `invoices/` route — so `/payment/invoices/` matched `payment_detail(payment_reference='invoices')` instead of `invoice_generation`, and 404'd via `get_object_or_404` failing to find a payment with that reference. Moved both single-segment patterns (`payment_detail`, `refund_payment`) to the end of `urlpatterns`, after every literal route.
- [x] **`payment/views.py required_payments_list` was a two-line stub** (`return render(request, 'finance/required_payments.html')`, no query, no context) despite `required_payments.html` being a fully-built DataTable + create/edit modal UI referencing `finance:required_payment_create/update/toggle/delete` URL names that didn't exist anywhere, plus a JS template-tag typo (`"{ url ... %}"`, missing the leading `%`, so even fixing the namespace wouldn't have made the create button work). Built the real list view (KPIs, `all_programs`/`all_courses`/`all_sessions`/`suggestions` context) plus `required_payment_create/update/toggle/delete` against `AllRequiredPayments`, registered under the `payments:` namespace (matching sibling payment-app pages), and fixed both template bugs. Note: `management` already has its own separate, working `required_payments_list`/`create`/`edit`/`delete` (Phase 3) under `management:` with its own template — that's the admin-oversight view; this is the finance-portal one linked from the finance sidebar. Two parallel surfaces over the same model, not a collision, but worth a product note if that duplication should be collapsed later.
- [x] `refund_payment` now reverts `CourseApplication.payment_status` back to `'refunded'` after a successful refund (was previously only updating `ApplicationPayment.status` — a refunded application kept reporting as fully paid via `CourseApplication.is_paid`, which `can_access_student_portal`'s payment gate reads). Closes quick-win #6 below.
- [x] `finance/forms.py DateRangeForm` declared a field named `range` with choices `7/30/90/365/custom`, but the dashboard view read `cleaned_data.get('range_type', ...)` and the template's `<select name="range_type">` submits `today/yesterday/this_week/.../custom` — the name *and* the choice set both mismatched, so `range_form.cleaned_data.get('range_type')` was always `None` and the date-range picker on `finance/dashboard.html` silently did nothing; the dashboard always showed "This Month" no matter what was selected. Renamed the field to `range_type` with the real choice set, added a `start_date <= end_date` `clean()` check for the custom-range case.
- [x] `finance/views.py finance_dashboard`'s currency-symbol lookup imported a class named `SystemConfig`, which doesn't exist anywhere in `eduweb/models.py` (the real settings models are `SiteConfig` and `SystemConfiguration`, neither of which has a currency field) — wrapped in a broad `except (ImportError, Exception): pass`, so it silently failed on every single request. Same dead-import pattern existed (harmlessly, since those two models *do* exist) around `FeePayment`/`AllRequiredPayments` — moved both to real top-level imports and deleted the now-pointless try/except; left the currency symbol as an explicit `'$'` placeholder with a comment explaining no institution-wide currency setting exists yet (building one is out of scope here — matches the audit's existing cross-cutting theme #4).
- [x] `finance/forms.py` had three fully dead form classes (`PaymentFilterForm`, `RefundForm`, `InvoiceGenerateForm`) — near-duplicates of the real, actually-imported versions in `payment/forms.py`; `finance/views.py` never imported any of the three. Deleted.
- [x] `payroll_management`'s create POST saved the new `StaffPayroll` row twice (`.save()` before attaching files, then `.save()` again) and had no duplicate-submission guard against `StaffPayroll`'s existing `unique_together = ['staff', 'month', 'year']` — a double-click or back-button resubmit would 500 with a raw `IntegrityError`. Merged into a single save wrapped in `transaction.atomic()` with the `IntegrityError` caught into a friendly message. Added the same `transaction.atomic()` treatment to `payroll_detail`'s status update and `payroll_delete`'s attachment-cleanup-then-row-delete.
- [x] `PayrollCreateForm`'s five attachment fields had no server-side file validation — only the widget's `accept=` hint (client-side, trivially bypassed). Added extension allowlist + 10MB size cap in `clean()`.
- [x] `support/views.py` had no forms.py and built model instances directly from raw `request.POST.get(...)` with no validation in several places that could 500 on ordinary bad input, not just adversarial input: `sla_save`'s `int(request.POST.get('first_response_hours', 4))` etc. crashed on a blank/non-numeric field; `announcement_create` passed `request.POST.get('expires_at')` straight into a `DateTimeField` (crash risk on an unparseable string, and a naive-datetime timezone warning even when valid); several `category_id`/`user_id`/`assigned_to_id`/`department_id`/`head_id`/`agent_id`/`escalated_to_id` lookups did `get_object_or_404(Model, pk=raw_value)` or `.filter(pk=raw_value)`, and Django's pk-type coercion raises a bare `ValueError` (not `Http404`) for a non-numeric or blank string, so these were guaranteed 500s on malformed input rather than clean validation errors. Added a shared `_parse_pk()` helper and applied it everywhere; `sla_save`/`announcement_create`/`kb_article_create`/`kb_article_edit`/`faq_create` now validate and degrade to a `messages.error` + redirect instead of crashing. Verified via test client: each of these now returns 200 with no DB writes on bad input, and still succeeds normally on valid input.

**Verification performed**: `python manage.py check` and `makemigrations --check` both clean throughout; every finance/support/payment page swept via Django test Client against real dev DB data (both a permission-restricted real finance user and a role-default one, via a rollback-wrapped matrix override, to confirm both the allow and deny paths); full create→toggle→delete round trip exercised against real `AllRequiredPayments`/`FeePayment` rows; refund flow re-verified to revert `CourseApplication.payment_status`; all breadcrumb overrides spot-checked for the two intentional exceptions (`finance/invoice.html` and `support/base_support.html`, both non-`base.html`-content pages).

**Still open, not touched this session**: `payment` app's `required_payments_list` duplication vs. `management`'s own copy (noted above, a product decision not a bug); consolidating the five hand-rolled role-gating decorators into `eduweb.decorators` (still `management` x3, `finance`, `payment`, `support`); currency is still a hardcoded `'$'`/`'USD'`/`'NGN'` mix with no real per-institution setting.

### Phase 5 — remaining "Recommended next steps" backlog closed (2026-07-14)

Worked through every remaining open item from the "Recommended next steps" section below, per explicit user request ("work on all of these listed things ensuring all works well"). Three product decisions were made up front: required-payment gating blocks **new course registration only** (not portal access, not already-registered courses); instructor course create/delete stays admin-only (dead UI removed rather than built out); the two parallel required-payments CRUD surfaces (`management` vs `payment`) get **collapsed into one** (the finance-portal one, richer UI, kept as canonical).

**Quick `eduweb` fixes — done:**
- [x] Deleted the dead first `SignUpForm.clean_email` (`eduweb/forms.py`) — the second definition (case-insensitive match) already won at runtime; just removed the shadowed dead one.
- [x] `SECRET_KEY`/`DEBUG` now fail closed: `SECRET_KEY` has no default any more (raises `ImproperlyConfigured` if `.env` doesn't set it, verified by temporarily stripping it from `.env` and confirming Django refuses to start), `DEBUG` defaults to `False` instead of `True`. Verified the real local `.env` already sets both explicitly, so no behavior change for actual dev/prod use — this only closes the "misconfigured/missing .env" failure mode.
- [x] `BlogPost.increment_views` switched from `self.views_count += 1; self.save()` to `F('views_count') + 1` via `.update()`, matching `LibraryItem`'s existing correct pattern.

**Academic progression correctness — discovered already fixed, tracker was stale:**
- [x] All four items (idempotency guard, closed-session restriction, `transaction.atomic()` batch wrapping, cross-session `failed_courses`) were already implemented in commit `5916ef0` ("academic progress") — this section of AUDIT.md just hadn't been refreshed to reflect it. Re-verified end-to-end via test client against real dev DB: an open-session confirm POST is rejected with the student left unchanged; resubmitting an identical confirm POST against a closed session creates exactly one `ProgressionDecisionLog` row, not two.
- [x] Backfilled pre-existing `CourseGrade` rows created under the old exam-blind logic — new one-time management command `python manage.py backfill_course_grades` (supports `--dry-run`) re-runs `CourseGrade.recompute_for_student_course` for every existing grade row. Run against real dev DB: 7 genuine corrections (mostly grades improving once exam scores were correctly incorporated), 47 already-correct, 31 skipped (no active enrollment). Confirmed idempotent — a second run reports 0 changes.

**Cross-app decorator duplication — narrower fix than the audit's phrasing implied:** the real duplication was just `is_finance_manager`, byte-for-byte identical in `finance/views.py` and `payment/views.py` — moved to `eduweb/decorators.py`, both apps now import the one copy. `management`'s `is_admin`/`is_staff_member` and `support`'s `support_required`/`support_admin_required` are each used only within their own app with deliberately different semantics (e.g. `management.is_admin` treats `is_staff` alone as a bypass; the pre-existing `eduweb.decorators.admin_required`/`finance_required` don't) — not actually duplicated anywhere, so left alone rather than risking a real access-control regression by forcing uniformity where the underlying rules are legitimately different.

**Required-payments registration gate (student) — done**, reusing the existing `_get_outstanding_for_student` (already powers the "My Payments" page) rather than hand-rolling a second copy of "what does this student owe": new `_overdue_required_fees()` helper in `student/views.py`, wired into `register_semester_course` only (not the finalize/enroll step, not portal access). Verified via test client: registering while an overdue fee exists is blocked with a message pointing at "My Payments" and no `CourseRegistration` row is created; the same request succeeds normally once the overdue fee is removed.

**A serious, previously-undetected bug found and fixed while wiring the above**: `eduweb/views.py` had a view function literally named `settings` (the shared account-settings page). Since Python resolves module globals at *call* time, not *def* time, this permanently shadowed `from django.conf import settings` for every function in the module from that point in execution onward — regardless of source position. This silently broke `settings.STRIPE_PUBLIC_KEY` in two places (`get_payment_summary`, `get_student_fee_summary`) **and, critically, `settings.STRIPE_WEBHOOK_SECRET` in the Stripe webhook handler** — meaning Stripe payment-confirmation webhooks have been silently failing, not "solid" as the eduweb section below still claims (that claim predates this discovery). Renamed the view to `account_settings` (URL name stays `'settings'`, so `{% url 'eduweb:settings' %}` is unaffected everywhere). Also fixed `get_student_fee_summary`'s two compounding bugs while in there: `.select_related('faculty', 'department')` on `AllRequiredPayments` `@property` methods (`FieldError` — the exact same class of bug as `get_student_fee_summary`'s sibling crash already fixed in Phase 1, just a second occurrence), and `settings.STRIPE_PUBLISHABLE_KEY` (doesn't exist; real name is `STRIPE_PUBLIC_KEY`). This endpoint feeds the student payment modal directly — it's what makes the new registration gate above non-dead-ending (a student blocked from registering can now actually pay the fee via the same modal, which previously 500'd on every use).

**Instructor dead UI — removed, not built out:** `course_create` (URL already commented out — fully unreachable) and `course_delete` (URL still registered but zero template links anywhere, `raise PermissionDenied` unconditionally) both deleted from `instructor/views.py` and `instructor/urls.py`. No template changes needed — the buttons that used to point at these were already gone by an earlier refactor (matches Phase 2's note that stale UI in this app tends to get "refactored away, not fixed in place").

**Required-payments CRUD duplication — collapsed:** `management`'s separate `required_payments_list`/`create`/`edit`/`delete`/`send_overdue_payment_reminders` (its own template, `AllRequiredPaymentsForm`) removed; `management:required_payments_list` is now a one-line redirect to the canonical `payments:required_payments_list` (kept so old bookmarks/links don't 404). The one feature `management`'s version had that the finance-portal one didn't — "Send Overdue Payment Reminders" — was ported over as `payments:required_payment_send_reminders`, now a button on `finance/required_payments.html`. Because the canonical page's outer gate (`is_finance_manager`) only ever allowed `role=='finance'` with no admin/superuser bypass, added a narrowly-scoped `can_manage_required_payments()` predicate (finance role OR admin OR superuser) used **only** by the five required-payments views — deliberately not broadening the rest of the payment app (payroll/subscriptions/refunds stay finance-role-only, unchanged). Discovered and fixed the same "admin role missing a StaffPermissionsMatrix row" gap already hit once before for `finance_payroll` (migration `0044`) — `admin` had no `finance_payments`/`finance_subscriptions` row at all, which would have silently blocked every non-superuser admin from create/edit/delete on the newly-canonical page; backfilled via migration `0048`. Verified end-to-end with the real non-superuser `femiadmin` account: old management URL redirects correctly, create/delete/send-reminders all work, finance-role access and student-blocking are both unaffected.

**Full regression sweep**: every role (student/instructor/admin/finance/support/superuser) × every major page swept via test client after all of the above — no unexpected status changes; the only 302s are the pre-existing, unchanged `is_finance_manager` no-bypass behavior for the general finance/payment pages (not touched this session, still worth a future decision on whether it's intentional).

### Phase 6 — `eduweb/views.py` + `eduweb/forms.py` senior-engineer pass, live browser testing (2026-07-14)

Full line-by-line read of `eduweb/views.py` (2200+ lines) and `eduweb/forms.py` against a production-quality checklist (security, race conditions, transactions, error handling, N+1s, performance), then verified with a real headless-browser session (Playwright + the Django dev server) driving login → OTP → dashboard and the finance/support/admin portals, not just the test client.

**A real, exploitable security vulnerability found and fixed**: `PasswordResetRequestForm.clean_email` raised a `ValidationError` — and so failed `form.is_valid()` — *only* when the submitted email was **not** registered, despite its own comment claiming the opposite ("don't reveal if email exists"). This completely undermined `forgot_password`'s otherwise-correct enumeration-safe design (`except User.DoesNotExist: pass`, same success screen either way) — an attacker could submit any address and immediately learn, from the page state alone, whether it belonged to a real account. Fixed: the existence check was removed from the form entirely (the view already does it safely); verified a real and a fake email now both render byte-identical "Check your inbox!" success screens.

**The reported "OTP resend isn't working" bug — root-caused and fixed without backgrounding email sends**:
- Root cause: `EMAIL_TIMEOUT` was set to **120 seconds**, so a slow/unresponsive SMTP connection held the login or resend request open that whole time — indistinguishable from a hang. Reduced to 15s (long enough for a normal send, observed 1-5s in practice; bounds the worst case to something a user will actually wait out).
- `send_otp_email` (`eduweb/emailservices.py`) caught all exceptions internally and never returned a success/failure signal, unlike its sibling `send_password_reset_email` — meaning both the initial login OTP send and the resend button always claimed success even when the email genuinely never sent. Fixed to return `True`/`False`; both call sites in `auth_page`/`otp_verify` now check it and tell the user honestly if sending failed (rolling back the just-generated OTP code on the resend path so the DB doesn't point at a code that was never delivered).
- `templates/otp_verify.html`'s resend handler silently did nothing on `success: false` (no error shown) and had no protection against a second click firing a duplicate send mid-request; both fixed (immediate disable + "Sending…" state, error toast on failure).
- **A background `Thread(daemon=True)` approach was tried first, then explicitly rejected by the user based on prior production experience**: on Passenger/shared hosting, the worker process can be recycled before a detached thread finishes, so backgrounded emails silently never send — which is why the codebase's emails are synchronous to begin with. All Thread-based attempts across `eduweb/views.py` (OTP send/resend, signup verification, password reset, contact form, document upload, application submission, admission acceptance, and the pre-existing one in `apply` for confirmation emails) were reverted back to synchronous, relying solely on the new 15s `EMAIL_TIMEOUT` to bound the wait. The dead `threading.Thread` import was removed once nothing referenced it any more.
- Verified end-to-end in a real browser (not just test client): full login → OTP email → resend → code entry → dashboard flow, screenshotted at each step; resend confirmed to actually regenerate and (in this sandbox's real SMTP config) successfully deliver a new code.

**Other real bugs found and fixed in `eduweb/views.py`**:
- "Remember me" checkbox never worked — template field is named `remember`, the view read POST key `remember_me` (always `None`). Fixed to read `remember`.
- `otp_verify`'s post-verification redirect was missing a `role == 'support'` branch (present in `auth_page`'s equivalent logic) — support staff landed on `eduweb:apply` instead of their dashboard after OTP login.
- `redirect_after_login()` — dead code, called from nowhere in the codebase, and its one non-trivial branch referenced a nonexistent `student:` URL namespace (real one is `students:`). Deleted.
- `contact_submit` had a guaranteed-crash bare `redirect('index')` (no `eduweb:` namespace) on any non-POST request.
- `create_payment_intent`'s application-fee Stripe charge used `currency='gbp'` while the `ApplicationPayment` row it creates in the same call always records `currency='USD'` — a real mismatch between what Stripe actually charged and what's on record, which would corrupt any future refund/reporting math. Aligned to `'usd'`, matching both the DB record and the student-fee branch alongside it.
- The Stripe webhook's application-payment branch (`elif payment_type == 'application':`) was **permanently unreachable dead code** — `create_payment_intent` never set `metadata['type'] = 'application'` on the PaymentIntent, only student-fee payments did. This is the fallback path for when a payment succeeds at Stripe but the browser-side `confirm_payment` call never reaches the server (closed tab, etc.) — it had no working fallback at all until now. Fixed by adding the missing metadata key.
- `accept_admission` now wrapped in `transaction.atomic()` + `select_for_update()` — previously a double-submit (double-click, two tabs) could pass the `admission_accepted` check twice and issue two admission numbers; verified a resubmit is now correctly rejected as "already accepted."
- `_build_countries_list()` (~200 countries + phone-code lookups via `pycountry`/`phonenumbers`) was rebuilt from scratch on every single GET to the `apply` page. Cached with `functools.lru_cache` — pure function, static reference data, safe to compute once per process.
- Two places (`apply`, `save_application_draft`) echoed the raw exception object back to the user in the JSON error response / Django message (`f'An error occurred: {exc}'`) — a real information-disclosure risk (internal field/table names, stack fragments). Both now log the real exception server-side via `logger.exception()` and show a generic, user-safe message instead.
- Deleted a stray, unused `from django.http import JsonResponse as jsonresponse` mid-file import.

**Also added**: email-verification tokens now expire (7 days — deliberately generous, since there's currently no self-service "resend" entry point for a not-yet-active account; `UserProfile.verification_token_created` + `is_verification_token_valid()`, migration `0049`, existing unverified accounts backfilled to a fresh 7-day window at migration time so nobody was retroactively locked out).

**Verification**: `python manage.py check` and `makemigrations --check` clean throughout. Installed Playwright + Chromium in this environment for real browser testing (not available previously); drove the dev server directly rather than relying solely on the Django test client for this pass.

---

### Phase 7 — final Thread revert, intake-None crash fix, matric number feature (2026-07-14)

**Closed out the one remaining background-email Thread flagged in Phase 6**: the `apply` view's confirmation-email pair (`send_application_confirmation_email` / `send_application_admin_notification`) was still wrapped in `Thread(target=_send_emails, daemon=True).start()` — the same Passenger-recycling failure mode already fixed everywhere else in Phase 6. Reverted to synchronous, same as every other email in the file; removed the now-fully-unused `from threading import Thread` import.

**Fixed the intake-`None` crash flagged in Phase 6 as "found but not fixed"**: `send_application_admin_notification` (`eduweb/emailservices.py`) called `application.intake.get_intake_period_display()` with no null-guard, raising `AttributeError` whenever an application's intake hadn't been assigned yet (safely caught by its own try/except, so it degraded to "notification not sent" rather than 500ing the request — but still meant admins got no email at all for those applications). Fixed with a conditional fallback string ("Not yet assigned"); verified via direct function call against an application with `intake=None`.

**Added: student matric number**, derived from the existing `admission_number` (e.g. `ADM-2026-10DEBA33`) rather than a new stored field:
- `CourseApplication.matric_number` (`eduweb/models.py`) — a `@property`, so format changes need no migration/backfill. Swaps the `ADM` prefix for the student's department code (`Department.code`, falling back to `GEN` if missing), reuses the year and unique hex tail already embedded in `admission_number`, and caps the result at `MATRIC_NUMBER_MAX_LENGTH = 15` chars — shrinking the unique tail first (down to a floor of `MATRIC_NUMBER_MIN_UNIQUE_CHARS = 4`) before ever truncating the whole string. Both constants and the format logic live in one place, clearly commented as the single spot to change if the format needs to change later.
- `eduweb/context.py`'s `student_counts` processor (already registered globally, already the source of the student academic-status-bar data) now also resolves the student's most-recently-accepted `CourseApplication` and injects `matric_number` — no view changes needed since every student-facing page already receives this context.
- Displayed in two places in `templates/management/base.html` (the shared layout used by every role): the sidebar's bottom user-info block, directly under the role label (student role only); and as a badge at the front of the student academic-status bar (session/level/registration-window strip), both guarded by `{% if matric_number %}` so it never renders for non-students or students without an accepted application yet.

**Verification**: logic unit-tested via a rolled-back DB transaction against real `Program`/`Department` rows across multiple department-code lengths (3, 5, 8 chars, and empty/missing code), all producing correct ≤15-char results. `manage.py check` blocked this session by a temporary tool-execution outage — re-run before considering this fully verified.

---

### Phase 8 — full cross-app re-audit, six-agent pass (2026-07-21, audit only — no fixes applied yet)

This repo went a week without a fresh audit while 9 more commits landed after Phase 7 (`1afe53d` through `80bdd21`, 2026-07-14 through 2026-07-20). Real Stripe money is now live in production, so the user asked for a full role-by-role re-audit — sidebar/functionality, cross-role integration, transcript flow, responsiveness, payment security — **before** any further refactor work, explicitly as a report, not a fix pass. Six read-only agents ran in parallel, one per app/domain, each re-verifying every prior AUDIT.md claim against current code and reviewing everything that changed since Phase 7. Nothing below has been fixed yet — it's the backlog for the next work session.

**Money path (priority, given real transactions are live)**

1. **[HIGH] Refund/failed-payment creates a permanent dead end.** `create_payment_intent` (`eduweb/views.py:1936-2027`) only special-cases `existing.status in ('success','pending')` before falling through to `ApplicationPayment.objects.create(...)`/`FeePayment.objects.create(...)`. Both models enforce uniqueness (`ApplicationPayment.application` is `OneToOneField`; `FeePayment` has `unique_together=[['user','fee']]`), so once a payment is refunded or marked `failed`, `.create()` always raises `IntegrityError` — caught by a broad `except Exception` that returns a generic 500. **A refunded student can never pay again through the normal UI.** Fix: update the existing row in place when status is `failed`/`refunded` instead of creating a new one, inside the existing `transaction.atomic()` block.
2. **[MED] Stripe API key is now a per-request-mutated global, not a constant.** Since commit `80bdd21`, `stripe.api_key` is reassigned from a DB-backed `PaymentGateway` row on every request (`eduweb/views.py:1916,2057`, `payment/views.py:522`) instead of being set once at import from `settings.STRIPE_SECRET_KEY`. None of the three Stripe SDK call sites pass `api_key=` explicitly — all rely on the mutated global, a TOCTOU hazard under any concurrency. Fix: pass `api_key=get_stripe_secret_key()` explicitly per call.
3. **[LOW-MED] Webhook DB mutations have no exception boundary.** `stripe_webhook`'s `payment_intent.succeeded` handling (`eduweb/views.py:2230-2296`) isn't wrapped in try/except — an unexpected `IntegrityError` (e.g. triggered by #1 racing a webhook retry) returns a raw Django 500 to Stripe with nothing logged locally. Fix: wrap in try/except + `logger.exception`, still return 500 so Stripe retries, but make the failure visible server-side.
4. **[LOW] No reconciliation view** diffing Stripe's payment history against local `ApplicationPayment`/`FeePayment` rows — if the DB-configured webhook secret (new since `80bdd21`) ever drifts from what's registered in the Stripe dashboard, failures are silent beyond Stripe's own dashboard.
5. Everything previously fixed — server-side amount trust, webhook signature verification, idempotent `get_or_create` on `gateway_payment_id`, refund correctly reverting `CourseApplication.payment_status`, currency alignment, permission gating on refund/finance views, no IDOR — **re-verified intact, no regressions.**

**eduweb core**
- New SMTP-account-config UI (`management/views.py:2044-2106`, commit `80bdd21`) is well-built: Fernet-encrypted at rest (`eduweb/models.py:30-53`), password never round-trips to the browser, gated by a real `is_superuser_only()` check that explicitly excludes matrix-granted staff. Same encryption pattern now backs the new DB-configurable `PaymentGateway` Stripe credentials. No issues found.
- OTP-resend fix from Phase 6 (return-value-checked `send_otp_email`, 15s `EMAIL_TIMEOUT`) confirmed not regressed. `SECRET_KEY`/`DEBUG` fail-closed behavior confirmed intact. `makemigrations --check --dry-run` clean.
- Note, not a bug: `0799e63` removed `UserProfile.save()`'s `is_staff == (role=='admin')` self-correction in favor of the new `StaffPermissionsMatrix`-driven cross-portal bypass model (see management, below) — a deliberate architecture change, flagged so it isn't mistaken for a future regression.

**`melbac` — undocumented app, needs a product decision, not a bug fix**
- A complete, actively-maintained (since March 2026) public site for a *separate* named institution ("Melchisedec Graduate Bible Academy"), live at `/melbac/`, sharing `Faculty`/`Program`/`BlogPost` data with `eduweb` but running an entirely separate front end and stylesheet (`static/css/melbac.css`, ~15k compiled lines). Not mentioned anywhere in `CLAUDE.md`.
- **Integration-orphaned**: zero links from any `eduweb` template into `/melbac/` (undiscoverable from the main site), and zero links back — its "Apply Now" CTA (`templates/melbac/base.html:129-133`) routes to a static `melbac:admissions` page with no form, never reaching the real `CourseApplication` pipeline. A visitor cannot apply, log in, or reach the student portal from this site.
- Otherwise well-built: real DB-backed content (not placeholder) for index/academics/blog/contact, CSRF-protected contact form, solid responsive layout.
- **Needs a decision**: is this meant to be a linked sibling site, a rebrand-in-progress, or intentionally standalone? Nothing to build until that's answered.

**management**
- The four recent commits **strengthen** matrix enforcement (`_has_permission`/`require_permission` added to ~40 previously-blind views: broadcasts, site content, testimonials, exam moderation, enrollments/certificates/badges/reviews, academic sessions, course categories) and add a real `is_superuser_only()` lock on system-config/branding/email-config/payment-gateway views. This is not the "reopened hole" the commit message "permission gate open" made it sound like.
- **[Confirm intent]** `ed7fcd3` gave `is_admin()` a fourth bypass — `StaffPermissionsMatrix.user_can_view_any()` (`management/views.py:275-295`) — meaning any role, including `student`, can pass `is_admin()` if granted `can_view` on an admin-portal module via a per-user matrix override row. `role_assign`'s student-exclusion was relaxed to just excluding self, and `_permission_modules_for_role` now returns all modules for any role rather than role-scoped ones. `QuickRoleChangeForm` still blocks an outright `role=student→staff` change server-side, so this isn't self-service escalation — but it's a real, widened trust boundary (a student account can be granted narrow admin-portal visibility without a formal role change). Reads as deliberate design (well-commented in `eduweb/models.py:3871-3874`), not an accident — flagging for an explicit yes/no rather than treating as a bug.
- Transcript issuance and progression (idempotency, closed-session restriction, atomic batching, GET-side permission check) all re-verified fully intact.
- **Follow-up needed**: confirm whether `finance`/`instructor`/`support`'s own gate functions actually consume the new cross-portal `user_can_view_any` mechanism yet, or still gate purely on `role==` — not re-verified in this pass for finance specifically (instructor confirmed it does, see below).
- Sidebar: all 44 `management:` URLs resolve, zero dead links.
- Still open, unchanged: `LMSCourse.category` FK still commented out; repeating students can still re-register for courses they've already passed at the same level — see student-app bug below, which management's progression feature depends on.

**student** — clean pass, no new findings. Grade-flow unification (four write paths → one `CourseGrade` → three consuming apps) re-verified structurally intact. Exam-taking mobile UI specifically confirmed solid (locked viewport, bottom slide-up question nav, always-visible timer, no horizontal scroll). Payment-gate (`is_paid`) and required-payments registration gate (`_overdue_required_fees`) confirmed to coexist correctly, not contradict each other.
- **[Still open, re-confirmed]** `student/views.py:_registrable_course_ids` (58-86) filters registrable courses by `year_of_study` or an open `CourseCarryOver`, but doesn't exclude courses at that level the student already has a passing `CourseGrade` for — a repeating student can still see and re-register for courses they already passed.

**instructor** — clean pass. Confirms the `require_permission` decorator added in `0799e63` (2026-07-16, previously undocumented) is real, matrix-backed enforcement on ~30 mutating views, not cosmetic — mirrors management/finance/support's pattern, backed by `SessionSecurityMiddleware._load_permissions`. Grading→`CourseGrade`→student-notification pipeline intact at all three entry points (assignment/quiz/exam). No regressions.

**library**
- **[LOW]** `detail()` view (`library/views.py:240-261`) doesn't gate access at all, unlike every other view in the app — an anonymous user with a direct link to a `members`-only item's detail page sees full title/author/description/publisher/cover. `download()` itself is still correctly gated, so no file/content leak, just a metadata-exposure inconsistency with the app's own binary access model. Fix: apply the same access filter `_base_qs()` uses elsewhere.
- Otherwise unchanged, still the cleanest app.

**support**
- Re-verified the Phase 4 "closed" claim and found it **strengthened**, not regressed: `0799e63` added GET-level `require_permission` checks (closing a direct-URL-bypass gap that survived Phase 4 — POST was gated, GET wasn't); `ed7fcd3` extended staff-role checks to also honor cross-portal matrix grants (same deliberate pattern as management's).
- Cross-role ticket flow confirmed genuinely wired: student/instructor/finance all funnel into the same `SupportTicket` table via one shared `submit_ticket` entry point (linked from `profile.html`, `compose_message.html`, and the shared sidebar for non-staff roles).
- **[Unfinished feature, flagged before, still true]**: `ChatSession`/`ChatMessage` has a list view and nothing else — no detail view, no message rendering. Build it out or remove the dead surface.
- **[Minor gap]** `category='payment'` tickets aren't auto-routed to finance staff; support agents handle everything manually.

**Uncommitted local state, noted not flagged**: `DigitalCampus/settings.py` currently has the DB engine reverted to sqlite3 with Postgres/MySQL blocks commented out — confirmed intentional local dev state directly with the user, not a bug.

#### Backlog from this pass (nothing below is fixed yet — audit only, per explicit request)

- [ ] `create_payment_intent`: handle `failed`/`refunded` existing-payment rows by updating in place instead of `.create()`-ing into a unique-constraint violation (`eduweb/views.py:1936-2027`)
- [ ] Pass `api_key=` explicitly on all three Stripe SDK calls instead of relying on a mutated global (`eduweb/views.py:1916,2057`, `payment/views.py:522`)
- [ ] Wrap `stripe_webhook`'s DB-mutating block in try/except + `logger.exception` (`eduweb/views.py:2230-2296`)
- [x] `library/views.py detail()` (240-261): apply the same access-control filter used elsewhere in the app — now builds its queryset via `_base_qs(request.user)` instead of a bare `is_active=True` filter, matching every other view in the app.
- [x] `student/views.py:_registrable_course_ids` (58-86): exclude already-passed courses from the same-level registrable set, not just carry-overs — added a `~Q(pk__in=passed_course_ids)` exclusion on the current-year branch only (the carry-over branch is untouched, since `CourseCarryOver.is_cleared=False` stays the authoritative "still needs retaking" signal). Verified against real dev DB: every current passing `CourseGrade` at the student's active level is now correctly excluded from `_registrable_course_ids`, and the one genuine open `CourseCarryOver` row in the DB (`priya_escalate`/CSC102, attempt 3) is still correctly offered. `register_semester_course`, `register_all_semester_courses`, and the dashboard credit-cap check all consume this same helper, so the fix closes the gap everywhere it was reachable, not just in `my_courses`.
- [ ] Product decision: what should `melbac` be — linked sibling site, rebrand path, or intentionally standalone? Nothing to build until answered.
- [x] Product decision: confirm `is_admin()`'s new per-user matrix-override bypass (`management/views.py:275-295`), which allows student accounts narrow admin-portal grants, is intended rather than scope creep — **resolved by hardening, not by confirming intent**: `StaffPermissionsMatrix.user_can_view_any()` (`eduweb/models.py:3878-3887`) now refuses the bypass outright for `role == 'student'`, regardless of any override row — this is the single choke point all four portal gates (`is_admin`, `is_finance_manager`, `instructor_required`, `is_support_staff`) route through, so a student account can no longer gain effective staff-portal access via a matrix grant. `management/views.py user_permissions` also now rejects saving an override row against a student target outright (`management/views.py:1747-1756`), so an admin can't be misled into thinking a grant took effect when it would have been silently inert.
- [ ] Follow-up check: do `finance`/`support` gate functions consume the new cross-portal `user_can_view_any` mechanism, or still gate on `role==` only? (`instructor` confirmed yes.)
- [ ] Build out or remove `ChatSession`/`ChatMessage` (support app) — currently a list view with no detail/reply capability
- [ ] Consider auto-routing `category='payment'` support tickets to finance staff

---

### Phase 9 — results-publish gate, credit-cap priority, program credit-unit admin UI (2026-07-21)

Three user-directed asks in one session: (1) confirm students can never see more than their own data or gain role/permission changes, (2) fix the carry-over registration bug, (3) make `Program.max_credits_per_semester` admin-editable instead of a de-facto-hardcoded default. A fourth, larger ask followed: gate visibility of compiled results behind explicit admin approval, and make current-semester courses take priority over carry-overs for the shared credit cap.

**Student data/sidebar scoping — audited, no exploitable gaps found.** A dedicated read-only pass over every `student/views.py` view, every sidebar conditional in `templates/management/base.html`, and every form touching `role` found: no IDOR (every query scoped to `request.user`/`student=`), no leaked staff-only sidebar links, and no student-reachable path that can write `role` (only `management.forms.UserProfileForm`, staff-only, includes it). One inert dead-code line flagged for hygiene (`templates/management/base.html:1476`, wrapped in `{% comment %}`, never renders).

**Student-eligible admin-portal bypass — closed, not just flagged.** `StaffPermissionsMatrix.user_can_view_any()` (`eduweb/models.py`) now hard-refuses the bypass for `role == 'student'` outright, regardless of any override row — the single choke point all four portal gates (`is_admin`, `is_finance_manager`, `instructor_required`, `is_support_staff`) route through. `management/views.py user_permissions` also now rejects *saving* an override against a student target, so an admin can't be misled into thinking a grant took effect when it would have been silently inert. (Already covered above under the Phase 8 backlog item.)

**`Program.max_credits_per_semester` — now actually admin-editable.** The field existed on the model (default 24, `MinValueValidator(1)`/`MaxValueValidator(30)`) but was missing from `ProgramForm.Meta.fields` (`management/forms.py`) — no UI anywhere could ever change it, so it behaved as a hardcoded 24 for every program in practice. Added to the form + widget, `templates/management/program_form.html` (new field in the Academic Structure section) and `templates/management/program_detail.html` (display). Verified via `ProgramForm(model_to_dict(program) | {'max_credits_per_semester': N}, instance=program)` against a real `Program` row: validates and saves correctly; reverted the test value afterward. `student/views.py` already reads this field everywhere the credit cap is enforced, so no further wiring was needed.

**Result-visibility gate — the dormant `CourseGrade.result_status` field (`pending`/`released`/`withheld`) is now the real "admin must approve before students see it" mechanism.** It already existed but only one place (`student/views.py dashboard`'s "recent grades" widget) ever honored it — `grades`, `academic_records`, `CourseGrade.compute_cgpa`, `CourseGrade.build_transcript_snapshot`, `CourseApplication.has_completed_program` (auto-graduation/certificate trigger), and `management.progression`'s promotion/carry-over-clearing logic all ignored it entirely, showing every compiled result the instant it was computed.
- [x] `CourseGrade.compute_cgpa` / `build_transcript_snapshot` / `CourseApplication.has_completed_program` now all filter to `result_status='released'` — a pending/withheld grade can no longer move CGPA, appear on a live or locked transcript, or auto-trigger graduation + certificate issuance.
- [x] `management.progression.compute_progression_decision`/`apply_progression_decision`: `core_passed`, the per-course `failed_courses` loop, and the carry-over-auto-clear check all now require `result_status='released'` — a promotion/probation/withdrawal decision can no longer be driven by a result the registrar hasn't signed off on.
- [x] `student/views.py`: `_registrable_course_ids`, `my_courses`'s own `passed_course_ids`, and `grades`'s `academic_grades` query all now filter/exclude on `result_status='released'` (a course an unpublished "pass" belongs to must not silently disappear from a student's registrable list, nor should the unpublished grade appear on Grades & Performance). Added a `pending_results_count` badge ("N result(s) awaiting registrar approval") to `templates/students/grades.html` so a hidden result reads as "not released yet," not as data loss.
- [x] New `CourseGrade.publish_results(session, term, program=None, status='released')` classmethod — single source of truth for the bulk state change, plain `.update()` (no recompute; publishing is a visibility action, not a re-grading one).
- [x] New admin console: `management:results_publish` (list of (session, term) groups with pending/released/withheld counts + bulk Release All/Withhold All) and `management:results_publish_detail` (per-student override within one group, e.g. withholding a single result pending a discipline case) — `templates/management/results_publish.html` / `results_publish_detail.html`. New sidebar link ("Publish Results") next to "Progression & Carry-Over" in `templates/management/base.html`.
- [x] New `StaffPermissionsMatrix` module `results_publish` (migration `0053_seed_admin_results_publish_defaults` seeds the `admin` role's row; `0054_alter_staffpermissionsmatrix_module` is the accompanying `choices` state migration) — **deliberately kept separate from `academic_progression`**: releasing results to the whole student body is a distinct, higher-blast-radius action than running level-progression decisions, so a staff member granted one doesn't automatically get the other.
- **A real bug found and fixed during this work, before it reached the user-facing behavior**: the first cut of `publish_results` treated `term=''` as "every term in this session" rather than an exact match on blank. Since `CourseGrade.term` is frequently blank in this dataset, a "Release All" click on the one visibly-blank-term row in the group table silently released *every* term in that session — verified this actually happened against real dev data (a targeted 2-row release turned into all 85 rows going to `released`). Fixed to always exact-match `term` (blank included, as its own bucket) before any further testing; re-verified with a real bulk release that rows outside the targeted (session, term) pair are provably untouched. The dev DB's `result_status` mix was regenerated afterward (`22 pending / 48 released / 15 withheld`, seeded via the same `random.choice` pattern `seed.py` originally used) since the original per-row 54/31 pending/released split couldn't be reconstructed — flagged here since it's a real, if low-stakes (dev/demo data only), side effect of the bug before the fix landed.

**Credit-cap priority for core courses over carry-overs/electives — `student/views.py register_semester_course`.** Previously the shared per-semester credit cap was enforced only against the course being added in isolation, so a student could greedily register carry-over/elective courses first and box themselves out of a mandatory core course later (no room left, yet `register_all_semester_courses` still requires every core course selected before it will finalize — a dead end). Added a reservation check: when the course being added is *not* core, the sum of CU still needed for the student's not-yet-registered core courses this term is reserved against the cap before the non-core add is allowed; core-course adds are unaffected (nothing to reserve against itself). Verified the underlying query against real program data (BSc AI, level 1: 12 CU of core reserved out of an 18 CU cap) and confirmed a normal elective registration still succeeds end-to-end when room exists (registered then cleaned up the test row).

**`my_courses.html` — split into two visually distinct sections**, not just an inline badge. `semester_courses` already lists current-term courses first with carry-overs appended (Phase 1 addendum); added a divider row via `{% ifchanged course.is_carry_over %}` (fires exactly once, at the real list transition — no duplicated row markup, no second loop) reading "Outstanding / Carry-Over Courses — register alongside your courses above if space remains." Verified via direct template render with a synthetic mixed course list: divider appears between the two groups in the correct order, and does not render at all when a student has no carry-overs.

**Carry-over courses locked until the current semester's own courses are fully registered — follow-up product decision.** After the above, the user clarified the intended order more strictly: a student must register every regular course this term (core, elective, or any other type) before a carry-over course can be added at all, not just have CU room reserved for core.
- [x] `student/views.py register_semester_course`: before any other check, if the course being registered has an open `CourseCarryOver` row, computes the full set of regular (non-carry-over) course IDs for the student's program/level/term and blocks the add with a count-bearing error message ("Register your N remaining course(s) for this semester before adding carry-over course...") unless every one of them already has a `pending`/`approved` `CourseRegistration`.
- [x] `student/views.py my_courses`: computes the identical `regular_courses_complete` boolean (before carry-overs are appended to `semester_courses`, so "regular" can't accidentally include them) and stamps each carry-over course with `locked_pending_regular` — kept as one shared computation shape between the two views so the "Add" button and the server-side block can never disagree.
- [x] `templates/students/my_courses.html`: a locked carry-over row now shows a greyed "🔒 Locked" state instead of a clickable "Add" button, and the section divider's subtitle switches between "locked until every course above is registered" and "register alongside your courses above if space remains" depending on `regular_courses_complete`.
- **Verified**: against the one real open `CourseCarryOver` row in dev data (`priya_escalate`/CSC102), confirmed 9 regular courses exist for that term with only 3 registered — i.e. this student's carry-over would correctly be blocked right now. Rendered the template directly in both states (incomplete vs. complete) via a synthetic course list and confirmed the Locked/Add toggle and divider copy both switch correctly; `manage.py check` clean, no migration needed (no model changes).

**Verification performed**: `python manage.py check` and `makemigrations --check --dry-run` clean throughout; every new/changed code path exercised against real dev DB data via Django test Client (`femiadmin`, the real non-superuser admin, for the new admin console; real students for grade-visibility gating and the credit-cap reservation); the `publish_results` term-matching bug was caught and fixed via this same real-data verification, not left for a future pass.

**Phase 9 addendum (2026-07-22) — credit cap became a real 3-tier hierarchy, not a flat per-program number.** The user correctly pushed back on the single `credit_caps_by_term` field added above: it was fixed to exactly three canonical term keys (first/second/annual), so it couldn't follow a session that genuinely defines a different number of terms, and couldn't let the *same* term name carry a different cap in a different academic year. Resolved by adding a third, most-specific tier:
- [x] New model `ProgramSessionCreditCap` (`program`, `session`, `term`, `max_credit_units`, `unique_together`) — one row per (program, session, term), so the number of caps a program can have for one session is however many terms that session's own `term_dates` actually lists, not a fixed enum. Migration `0056_programsessioncreditcap`.
- [x] `Program.max_credits_for_term(term, session=None)` rewritten as a proper 3-tier lookup: `ProgramSessionCreditCap` (exact session + **raw** term) → `credit_caps_by_term[normalised term]` → `max_credits_per_semester`. The raw-vs-normalised distinction matters: a session with a real `'third'` term must be looked up as `'third'` at tier 1 (so it can get its own cap, not silently reuse second's), while tier 2 still only understands the three canonical names it was built with.
- [x] **Found and fixed a real duplication while doing this**: `student/views.py` had its own private copy of the international/alias term-normalisation map (`term_normalisation_map`), needed again by the new model method — a model can't import from a view module, so the map was moved to `eduweb.models.TERM_NORMALISATION_MAP` (the one canonical copy) and `student/views.py` now imports it instead of hand-rolling a second one.
- [x] All three `student/views.py` call sites (`my_courses`, `register_semester_course`, `register_all_semester_courses`) now pass the **raw** term (not pre-normalised) plus `session=`, since normalisation now happens inside the model method itself, scoped to the correct tier only.
- [x] Admin UI: `management/views.py program_create`/`program_edit` gained `_sync_program_session_credit_caps` (parses `session_cap_<session_id>_<term>` POST fields, upserts/deletes rows) and `_program_sessions_credit_cap_context` (one entry per `AcademicSession`, listing only the terms that session actually defines). `templates/management/program_form.html` gained a "Per-Session Credit Unit Overrides" card below the per-term one; `program_detail.html` displays any session-specific overrides as badges.
- **Verified**: the full fallback chain directly (tier 1 beats tier 2 beats tier 3; a raw `'third'` term finds its own session-specific cap without being collapsed to `'second'`, but still normalises correctly at tier 2 when no session-specific row exists); the real admin form end-to-end (GET renders the per-session inputs, POST creates/updates the `ProgramSessionCreditCap` row via the live endpoint, `max_credits_for_term` picks it up immediately); no leftover test rows, `manage.py check`/`makemigrations --check` clean throughout.

---

### Phase 10 — two-agent audit of `student` + `management`, high-severity self-regression caught (2026-07-21)

Two parallel read-only agents each fully audited one app — read every view/URL/template, then live-tested against the real dev DB via the Django test Client (multiple real student personas for `student`; both `super` and the real non-superuser `femiadmin` for `management`, since permission behavior genuinely differs between them). Findings below were then triaged and the high-severity ones fixed and re-verified in the same session, not left as a backlog-only report.

**`student` — two HIGH bugs found in Phase 9's own carry-over-lock feature (fixed same session).** Root cause in both: when an open `CourseCarryOver` course happens to share the student's *current* `year_of_study` (i.e. she's repeating that level, the single most common real case), it landed in the ordinary current-level query and was then excluded from the dedicated carry-over query for "already being present" — so it never got `is_carry_over=True` stamped, and the `regular_course_ids` "must register these first" set included the course as one of its own prerequisites, plus any already-passed course as an unregisterable one — a mathematically permanent deadlock. Live-reproduced exactly as the audit found it (`priya_escalate`/CSC102: 9→4 remaining, could never reach 0) before fixing.
- [x] `student/views.py my_courses`: rebuilt as one unified query — current-level courses (excluding passed) UNION any open carry-over regardless of level — with `is_carry_over` determined solely by `CourseCarryOver` membership, never by which query a course happened to come from. `regular_course_ids` now excludes anything flagged `is_carry_over`. Carry-over rows are sorted after regular ones (stable sort) so the existing `{% ifchanged %}` divider still fires correctly.
- [x] `student/views.py register_semester_course`: `regular_course_ids` now excludes both `passed_course_ids` and `open_carry_over_course_ids`, computed once and reused by the adjacent core-CU-reservation check too (which had the same passed-course over-reservation bug).
- **Verified end-to-end** by calling the real (undecorated) `my_courses` view directly for `priya_escalate`: CSC102 now renders with its "Carry-over — failed 3x previously" badge, in the Outstanding section, showing "🔒 Locked". Registered all 7 real regular courses for the current session (temporarily) and confirmed the row switches to a live registration form; deleted the temporary registrations afterward and confirmed the account's registration history matches its original 7-row state exactly.

**`student` — one MEDIUM bug found and fixed: `course_detail.html` instructor name always blank.** `templates/students/course_detail.html:184,196` referenced `course.instructor_name`, a commented-out `LMSCourse` field; every sibling template (`course_catalog.html`, `dashboard.html`) already guards against this and falls back to the real `course.instructor`/`course.lecturer` FK, but this one didn't. Fixed to use `course.instructor.get_full_name|default:course.instructor.username` (the same FK the avatar image on the same block already correctly uses), with a "To be announced" fallback when no instructor is assigned. Verified live: `talia`'s Mobile App Development course now renders "Abraham Olufemi" instead of blank.

**`student` — LOW findings, not fixed, tracked as backlog:**
- [ ] `quiz_take` (`student/views.py:2453`) creates a new `QuizAttempt` row on every GET with no check for an already-open incomplete attempt — harmless for scoring (everything filters `is_completed=True`) but accumulates orphaned rows on repeated navigation.
- [ ] `study_group_detail` (`student/views.py:2939`): a POST from a non-member silently falls through to the GET-render branch with no error message shown (not exploitable, just a silent no-op instead of user feedback).
- [ ] Seed-data gap (not a code bug): `marcus_repeat`/`priya_escalate`/`jordan_processed` have zero `CourseApplication` rows, so they 100%-redirect to `/admission/apply/` through the real view layer — any future browser-level test against these fixtures needs a temporary application first (as this pass had to do).

**`student` — confirmed clean, no new findings:** dashboard/courses/grades/progress/assignments/quizzes/community/study-groups/achievements/certificates/payments/academic-records/inbox/notifications all render real, non-empty content across 4 real accounts; payment gate and no-application gate both fail closed on every URL; no IDOR (message_thread, submit_assignment, quiz_take all correctly scoped); core-course drop guard holds; exam endpoint gating unchanged and correct.

**`management` — one HIGH bug found and fixed: `broadcast_send` still backgrounded real email via a daemon `Thread`.** `management/views.py:2907-2944` (pre-existing, not part of this session's earlier edits) — the exact Passenger-worker-recycling failure mode already identified and fixed everywhere in `eduweb/views.py` (Phase 6/7) had one surviving gap here. Worse than a simple dropped email: the row flips to `status='sending'` *before* the thread starts, with no timeout/watchdog, so a mid-send worker recycle leaves the broadcast permanently stuck "in progress" — the view's own re-entry guard treats `sending` as already-active forever, with no retry path.
- [x] Made synchronous, matching every other email send in the codebase — same batch/failure-handling logic, just inline instead of in a background thread. Removed the now-unused `threading` import (its only call site).
- **Verified live** (superuser, locmem email backend, a temporary draft `BroadcastMessage`): POST now completes synchronously, `mail.outbox` shows all 3 recipients, `status` lands directly on `sent` (never observably stuck on `sending`), success message reflects the real outcome. Temporary broadcast deleted afterward.

**`management` — findings surfaced, tracked as backlog (not fixed — need a product decision or bigger build, same treatment as the Phase 8 `is_admin()` bypass):**
- [ ] **[Decision needed]** The permissions-modal "View" checkbox (eye icon, labeled "View") is only enforced on GET for 2 of 28 modules (`academic_progression`, `results_publish`) — every other list/detail view (departments, faculties, programs, applications, enrollments, certificates, badges, blog, LMS courses, audit logs, library, payroll, etc.) is gated by role alone (`is_admin()`), not the matrix. Verified live against `femiadmin`'s real matrix row (`can_view=False` on ~26 modules) — all of those pages still return full data. `require_permission`'s own docstring documents `skip_get=True` as deliberate convention, so this isn't a hidden regression, but it directly contradicts what the checkbox visually promises. Needs an explicit yes/no like the `is_admin()` bypass got.
- [ ] `enrollment_edit`/`enrollment_delete` (`management/views.py:4483-4552`) are fully built (validation, notifications, dependent-record guard) but completely unreachable — `enrollments_list.html` has no Actions column at all, and no `{% url %}`/JS-path reference exists anywhere else either. Same "orphaned view" pattern already fixed for course categories, old course CRUD, and transcript issuance in Phase 3.
- [ ] Dead code tail in `send_department_approval_email` (`management/views.py:3168-3172`) — an unreachable no-op tuple expression after the function's own `try/except` (both branches return), plus an orphaned comment header with no code beneath it. Hygiene only.
- [ ] `dashboard`'s 7-day chart (`management/views.py:522-528`) does 7 separate `.count()` queries in a loop — same N+1 shape already fixed for the student 28-day heatmap in Phase 1. Low impact (admin-only, low traffic).
- [ ] `user_change_role`'s non-AJAX failure path (`management/views.py:1642-1645`) shows no error message on a rejected role change — confirmed dead in practice, since the only real caller (`role_assign.html`) always sends the AJAX header.
- [ ] One stale `StaffPermissionsMatrix` row for `role='admin', module='support'` — `'support'` predates the `support_tickets`/`support_knowledge_base`/etc. split and no code checks it; harmless, cleanup-only.

**`management` — confirmed clean, no new findings:** all ~145 view functions carry `@login_required` + a role gate (no unguarded views, including `financial_analytics`); delete-cascade guards on faculty/department/program/course/enrollment/badge/intake all real and dependent-count-checked (contradicting stale pre-Phase-3 AUDIT.md lines); zero dead `{% url %}` references across all 44 `management:` URLs; all 10 `except Exception` sites log properly and never leak internals; no module-name mismatches between `_has_permission` call sites and `StaffPermissionsMatrix.MODULE_CHOICES`; `results_publish`/`results_publish_detail` (this session's own new feature) re-verified live — term isolation holds, double-release is a safe no-op, `AuditLog` rows created correctly.

**Verification performed**: `python manage.py check` and `makemigrations --check --dry-run` clean throughout; both HIGH fixes verified live against real dev-DB data (or a temporary row deleted immediately after); no test mutation left in the database in either app.

---

### Phase 11 — two-agent re-audit after the 3-tier credit cap + bulk-volume reseed (2026-07-22)

Same two-agent pattern as Phase 10, re-run specifically to answer: does the credit cap actually fetch dynamically everywhere now, does the management app's new per-session admin UI actually persist correctly, and does everything still hold up under the `seed_bulk_volume` dataset (courses 34→263, LMS courses 17→190, programs 12→43, departments 13→33, sessions 3→6)?

**Credit-cap dynamic fetch — confirmed YES, mechanism is correct, but found a real HIGH bug in its input data (fixed).** `grep` confirmed zero remaining reads of `profile.program.max_credits_per_semester`/`credit_caps_by_term` anywhere in `student` — all three enforcement sites correctly call `max_credits_for_term(raw_term, session=...)`. Live boundary-tested: set a session-specific override to 12 CU, POST that exceeded it → correctly blocked with the exact overridden number in the error copy; raised it to 13 → same POST succeeded. The tier system itself works.

**But: `passed_course_ids` (used by `_registrable_course_ids`, `my_courses`, and the carry-over-lock check in `register_semester_course`) had no session scoping at all — HIGH, fixed.** `CourseGrade.recompute_for_student_course` runs continuously off live quiz/assignment/exam scores, so a course can legitimately read `is_passed=True` (and, if released early, `result_status='released'`) *mid-session*, before that session closes — not just from a genuinely completed prior session. Because none of the three `passed_course_ids` queries excluded the *current* session, a course could be simultaneously "currently registered" and "already counted as passed," silently dropping it from `registrable_course_ids` — which is what scopes the credit-cap SUM. Measured live: all 7 real student accounts had this exact collision post-reseed; for `abfembest2@gmail.com` the live `registered_credit_total` read **11 CU** when the true total was **46 CU** — an undercount that could let a student exceed their real cap without the system ever noticing, since the check was silently working off half the actual number.
- [x] `_registrable_course_ids(profile, session=None)` — new optional `session` param; the "already passed" filter now `.exclude(session=session)` when given, so a pass from the *current* session/term no longer hides an active registration, while a pass from a genuinely prior session still correctly excludes the course from future registration.
- [x] `my_courses`'s own separate inline `passed_course_ids`, and `register_semester_course`'s separate inline `passed_course_ids` (used by the carry-over lock check), given the identical `.exclude(session=...)` treatment.
- [x] All three `_registrable_course_ids(profile)` call sites (`dashboard`, `register_semester_course`, `register_all_semester_courses`) updated to pass `session=current_session`.
- **Verified**: recomputed `abfembest2@gmail.com`'s `registered_credit_total` before/after — 11 → 46, matching the true sum of pending/approved registrations. Confirmed the one genuine open `CourseCarryOver` (`priya_escalate`/CSC102) and the carry-over-lock feature (Phase 10) both still behave correctly against the 263-course catalog (no cross-program leakage, `regular_course_ids` count unaffected by the fix since her collision case doesn't apply to her).

**`student` — two MEDIUM query-cost findings, newly exposed by the bulk reseed — both fixed in Phase 12 below:**
- [x] `quiz_list` (`student/views.py`): 4 separate queries per quiz in a loop — measured 72 of 95 total queries on `/student/quizzes/` for 18 quizzes. Invisible at the old ~17-LMS-course volume, now real.
- [x] `templates/students/progress.html:102-103,131` independently calls `enrollment.course.lessons.first`/`.count` directly, bypassing the view's own bulk-computed `total_lessons_by_course` (`student/views.py`) meant to eliminate exactly this — re-introduces the N+1 the view already fixed once. Measured 58 extra queries on `/student/progress/` for one real student.
- [x] `templates/students/course_detail.html:72` — a second instance of the dead `course.instructor_name` field (Phase 10 only fixed the visible avatar block at lines 184/196), this one inside the `application/ld+json` schema.org block. SEO-only, not user-visible.

**`management` — per-session credit-cap admin UI confirmed working, including the non-superuser path.** Live-verified as both `super` and the real non-superuser `femiadmin`: set a per-term default (tier 2) and a per-session override (tier 1) via the real form POST, confirmed `Program.max_credits_for_term()` returns the right tier for each combination, confirmed re-opening the edit page shows the saved values back (not reset to blank), confirmed `femiadmin`'s existing real permission restriction (`academics.can_edit=False`) correctly blocks the save until granted. None of the 6 real `AcademicSession` rows currently define a 3+-term session, so the raw-vs-normalised distinction at tier 1 is verified by code inspection only, not yet exercised by real 3-term data.

**`management` — one HIGH bug found and fixed: `/management/student-badges/` 500s for every admin, right now, both roles.** `templates/management/student_badges.html` rendered `{{ sb.awarded_by.get_full_name|default:sb.awarded_by.username }}` — `StudentBadge.awarded_by` is nullable, and unlike a plain variable, an unresolvable *filter argument* (`sb.awarded_by.username` when `awarded_by` is `None`) raises instead of failing silently, crashing the whole page. 63 of 64 real `StudentBadge` rows (almost certainly from `seed_bulk_volume`) have `awarded_by=None`, making this a live, guaranteed crash, not a theoretical one.
- [x] Fixed with the same `{% if x %}...{% else %}System{% endif %}` guard this codebase already uses correctly elsewhere (`exam_detail.html`, `announcements_list.html`).
- [x] **Found and fixed the identical latent pattern before it could bite**: `templates/management/broadcast/list.html` (two occurrences — one plain HTML, one inside an inline `<script>` JS object literal) had the exact same unguarded `created_by.get_full_name|default:created_by.username`, and `BroadcastMessage.created_by` is equally nullable. Zero `BroadcastMessage` rows exist today so it hadn't fired yet — fixed pre-emptively with the same guard pattern (careful, for the JS-embedded one, to use `{% if %}` rather than nested quoting, avoiding the exact quote-collision bug caught and fixed in Phase 9's `results_publish.html`).
- **Verified live**: `/management/student-badges/` now 200 (was 500); `/management/broadcast/` renders cleanly.

**`management` — one MEDIUM N+1 found and fixed: `intakes_list`, 80 extra queries for a 20-row page.** `CourseIntake.accepted_count`/`remaining_slots`/`is_full` each independently ran `self.applications.filter(status='approved').count()` — a filtered aggregate that can't use the view's own `.prefetch_related('applications')` — and the template calls this chain 4 times per row (`is_full` twice, `remaining_slots` once, `accepted_count` once directly), each a fresh query.
- [x] `accepted_count` converted from `@property` to `@cached_property` — computed once per instance per request, reused by all 4 call sites on that same row. Safe here specifically: nothing in the codebase creates an application against an intake and re-checks these on that same already-loaded instance within one request.
- **Verified live**: `/management/intakes/` (now 86 real intakes) renders in 46 total queries, confirmed 200.

**`management` — two MEDIUM findings, real but not yet fixed (backlog, need a UX decision, not a quick patch):**
- [ ] `courses_list` (263 rows) and `lms_courses_list` (190 rows) have zero server-side pagination — the full queryset renders to the template every time for client-side JS search/filter. Measured live: 684 KB / 2.23s and 819 KB / 2.95s respectively. Correct, nothing truncated, but a real page-load regression directly caused by the volume increase — fixing this properly means deciding whether to keep client-side search (requiring all rows) or move to server-side pagination + search (a bigger change to the existing JS).
- [ ] `_sync_program_session_credit_caps`'s POST-key parsing (`session_cap_<id>_<term>`, split on `_` with maxsplit 3) assumes the term name has no underscore — true for all 7 `AcademicSession.TERM_CHOICES` values today, but `term_dates` is free-form JSON, not constrained to those choices. Low risk today, worth a delimiter change (e.g. `session_cap|<id>|<term>`) if custom term names are ever expected to contain underscores.

**Verification performed**: `python manage.py check` and `makemigrations --check --dry-run` clean throughout both agents' work and all fixes; every fix verified live against the real dev DB (query-count captured via `CaptureQueriesContext` where relevant); no test data or scratch files left behind by either agent or by the fix pass.

---

### Phase 12 — full `student` app deep pass: audit + cleanup + fix, one session (2026-07-22)

A single-session combined audit-and-fix pass over the entire `student` app (`student/urls.py`, `student/views.py` end-to-end across every function, `student/forms.py` end-to-end, all 31 templates under `templates/students/`), explicitly requested as fix-as-you-go rather than report-only. Read the full `AUDIT.md` history first (Phases 1, 5, 6, 8, 9, 9-addendum, 10, 11) to avoid re-diagnosing what was already found — this pass closes the three items Phase 11 left as diagnosed-but-not-fixed backlog, does a dedicated dead-code sweep, and re-verifies every view against real dev-DB data via the Django test Client across all 7 real student accounts (`abfembest2@gmail.com`, `talia`, `gbofag`, `ceceh`, `marcus_repeat`, `priya_escalate`, `jordan_processed`).

**The three Phase 11 backlog items — all fixed:**
- [x] **`quiz_list` N+1 (`student/views.py`)** — replaced the per-quiz loop's 4 separate queries (`.count()`, `.aggregate(Max('percentage'))`, `.order_by('-completed_at').first()`, `.filter(passed=True).exists()`) with one bulk `QuizAttempt.objects.filter(quiz_id__in=..., student=..., is_completed=True).order_by('quiz_id', '-completed_at')`, grouped into a `dict` by `quiz_id` in Python, then `len()`/`max()`/first-element/`any()` per quiz from the in-memory list. **Verified live** via `CaptureQueriesContext`: `/student/quizzes/` for `abfembest2@gmail.com` now renders in 24 total queries (was measured at 95 for 18 quizzes pre-fix); page content re-checked for `Passed`/`Not Started`/attempt data still present and correct.
- [x] **`templates/students/progress.html:102-103,131`** bypassing the view's own bulk fix — `student/views.py progress()` now also bulk-computes `first_lesson_by_course` (one query: `Lesson.objects.filter(course_id__in=course_ids, is_active=True).order_by('course_id', 'section_id', 'display_order')`, first-seen-per-course kept via `setdefault`, replicating `Lesson.Meta.ordering = ['course', 'section', 'display_order']` — the same ordering `enrollment.course.lessons.first` used to get for free, restricted to active lessons) and attaches `enrollment.first_lesson` / `enrollment.total_lesson_count` to each enrollment alongside the pre-existing `completed_lessons`/`progress_percentage`. Template swapped to `{% if enrollment.first_lesson %}` / `enrollment.first_lesson.slug` / `enrollment.total_lesson_count`. **Verified live**: `/student/progress/` now 32 total queries (was measured at "baseline + 58 extra" pre-fix); confirmed real non-zero lesson-total numbers (`6, 6, 6, ..., 2, 2, 5`) and a working "Continue Learning" link still render in the page content, not just a 200 status.
- [x] **`templates/students/course_detail.html:72`** — the second dead `course.instructor_name` reference (inside the `application/ld+json` schema.org block; Phase 10 only fixed the visible avatar block at lines 184/196). **First attempt at this fix was itself broken and was caught and corrected by the orchestrating session, not this pass** — the initial change to `course.instructor.get_full_name|default:course.instructor.username|default:"To be announced"|escapejs` only chained a third fallback onto the same crash-prone pattern; its own "verification" only exercised the case where `course.instructor` is set, never the `None` case the bug is actually about. Direct proof it still crashed: rendering the exact string via `django.template.Template.render()` with `instructor=None` raised `VariableDoesNotExist: Failed lookup for key [username] in None` — a `default:` filter's *argument*, when itself an unresolvable variable chain, raises instead of failing silently (the identical mechanism behind the `student_badges.html`/`broadcast/list.html` crashes fixed in Phase 11). Corrected to the same `{% if course.instructor %}...{% else %}To be announced{% endif %}` guard used everywhere else this pattern occurs, then re-verified with the same direct `Template.render()` test (now renders `'To be announced'` for `None`, the real instructor name otherwise) plus a live page hit for a real enrolled course with a populated instructor.

**Dead-code / redundancy sweep — `student/views.py` and `student/forms.py` read in full, cross-repo grep run before touching anything:**
- [x] **Deleted** `_get_eligible_exams()` (`student/views.py`, was lines 204-232) — a full duplicate of the "published exams this student is enrolled in, scoped to the current session/term" query that `exam_list` already implements inline. `grep` across the entire repo (not just `student/`) confirmed the only two matches for the name were its own `def` line and nothing else — zero callers anywhere, not a Django-required hook (not a signal receiver, not admin-registered, not referenced from any template or URL).
- [x] **Deleted** `_cert_is_unlocked()` (`student/views.py`, was lines 3473-3475) — a one-line helper (`cert.payment_status == 'paid'`) never called; both real call sites (`certificates()` and `certificate_print()`) already inline the identical check directly (`cert.is_unlocked = cert.payment_status == 'paid'` / `if cert.payment_status != 'paid':`). `grep`-confirmed zero callers repo-wide.
- [x] **Removed unused imports** from `student/views.py`: `CourseCategory` (imported from `eduweb.models`, never referenced anywhere in the file — `grep` confirmed), `from django.core.mail import send_mail` (never called; all real emails in this app go through `eduweb.emailservices` helpers), `from django.conf import settings` (never referenced — `grep` for `settings\.` and bare `settings` found zero real usages, only the unrelated URL name `eduweb:settings` inside a comment).
- Reviewed but **left alone** (working, not redundant): `models.Q(...)` usage alongside a directly-imported `Q` — a minor style redundancy (two ways to reach the same class) but both are actually used at different call sites, not dead; the several function-local `from .forms import X` / `from django.urls import reverse` imports duplicating top-of-file imports — harmless, pre-existing style, not touched per the "don't restructure working code" rule.

**Full view/template context-variable cross-check** — read every view function against its template (both directions: view provides everything the template uses, template doesn't reference anything the view forgot to set). No new mismatches found beyond the three already fixed above; every context dict passed to `render()` was traced to real template usage.

**Live verification — Django test Client, real dev SQLite DB, all 7 real student accounts, no fixtures:**
- All 16 no-argument GET pages (`dashboard`, `my_courses`, `course_catalog`, `assignments`, `quiz_list`, `exam_list`, `community`, `study_groups`, `achievements`, `grades`, `progress`, `certificates`, `my_payments`, `academic_records`, `inbox`, `notifications_view`) hit for all 7 accounts. `abfembest2@gmail.com`/`talia`/`ceceh` (paid, approved applications) return 200 on every page with non-trivial response bodies. `gbofag` (approved application, `payment_status='pending'`) correctly 302s on every page — re-confirms the Phase 1 payment gate is still intact, not a new bug. `marcus_repeat`/`priya_escalate`/`jordan_processed` (zero `CourseApplication` rows — a known seed-data gap flagged in Phase 10) correctly 302 to the application flow on every page.
- Per-object pages exercised for the three fully-active accounts with real owned objects: `course_detail`, `lesson_view`, `assignment_detail`, `quiz_detail`, `thread_detail` (GET + reply POST), `study_group_detail` (GET + message POST), `message_thread` (GET + reply POST), `mark_notification_read`, `certificate_print` — all 200/302 as expected. `talia`'s `certificate_print` correctly 302s to `my_payments` (her cert's `payment_status='unpaid'`), proving the payment-lock branch still works.
- **IDOR checks**: `message_thread` against another student's message ID correctly 302s with an access-denied message instead of rendering; `mark_notification_read` against another student's notification ID correctly 404s. No cross-student data leakage found on either mutating or read path.
- **Mutating endpoints, happy path + edge case, all wrapped in a rolled-back `transaction.atomic()` savepoint so nothing persists**: `mark_lesson_complete`; `submit_assignment` (happy path succeeds, and a too-short submission text is correctly rejected by `AssignmentSubmissionForm` with zero `AssignmentSubmission` rows created — verified by an exact before/after count, not just status code); `submit_review` (valid 1-5 rating succeeds, an out-of-range rating=9 is rejected with the real validation message rendered in the page and confirmed via direct `Review.objects.filter(rating=9).exists()` == `False`); `quiz_take` + `quiz_submit` (fresh attempt created, empty-answer submission correctly grades to 0%); `join_study_group` against a group the user already belongs to (correct "already a member" no-op); `create_thread`; `create_study_group`; `compose_message`; `drop_semester_course` on a real non-core registration; `register_semester_course` re-registering an already-registered course (correct info message, no duplicate row); `register_all_semester_courses`; `notifications_view?mark_all=1`.
- **Full exam flow, both the never-attempted path and the already-submitted path**: `abfembest2@gmail.com` already had a real `graded` `StudentExamResponse` for the one exam every seeded student shares (`introduction-to-computer-science-...-end-of-semester-exam`) from realistic seed data — first attempt at testing this flow inadvertently (and correctly) exercised the *already-submitted* guard end-to-end: `start_exam` redirected away, `submit_exam` returned `{'status': 'already_submitted'}`, `save_answer` was rejected. Re-ran with the existing response row deleted inside the same rolled-back transaction (so nothing real was touched) to exercise the genuinely fresh path: `exam_instructions` → `start_exam` (200, assigns questions, sets `IN_PROGRESS`) → `get_exam_data` (200, returns real question/option JSON) → `save_answer` (200) → `flag_tab_switch` (200) → `submit_exam` (200, auto-grades to a real percentage, `fully_graded=True`) → resubmitting the same exam correctly returns `already_submitted` instead of re-grading. Separately verified `save_answer` after `exam.end_datetime` has passed is rejected with `400` + `"The exam time window has ended."`, matching the Phase 1 addendum fix.
- Every mutation-bearing test above ran inside `transaction.atomic()` blocks that always roll back (via a deliberately-raised sentinel exception caught outside the `with` block) — this correctly unwinds nested `atomic()` blocks the views themselves open (registration, assignment submission, quiz submission all wrap their own writes in `transaction.atomic()`), so the outer rollback still reverts everything.

**Verification performed**: `python manage.py check` and `python manage.py makemigrations --check --dry-run` both clean after all fixes. Model row counts for every table touched by any test (`CourseRegistration`, `Enrollment`, `AssignmentSubmission`, `QuizAttempt`, `QuizResponse`, `Message`, `Notification`, `Review`, `StudyGroup`, `StudyGroupMember`, `StudyGroupMessage`, `Discussion`, `DiscussionReply`, `StudentExamResponse`, `CourseGrade`, `CourseApplication`) captured before and after the full test run and confirmed byte-for-byte identical (141/107/103/97/444/16/93/13/2/10/2/6/12/104/136/4 respectively, unchanged). All four temporary verification scripts (`_audit_student_verify*.py`, written to the repo root to drive the test-client sweep) deleted after use — not part of the app, not committed.

---

### Phase 13 — independently-drifting mechanisms connected: carry-over reconciliation, session/progression guards (2026-07-22)

Prompted by a design discussion, not a bug report: the user asked whether term-matching (date-driven, automatic) and academic progression (admin-triggered, manual) were supposed to run on "the same clock." They aren't, by design — but stepping back and asking "what else in this system looks connected but is actually two independent mechanisms with no enforced ordering between them" surfaced three real, concrete gaps, all following the identical shape: a live/computed truth sitting next to a stored snapshot of it, kept in sync only by a manual batch action, with nothing checking they haven't drifted apart before something acts on the stale one. A fourth candidate (`is_staff` unconditionally bypassing `StaffPermissionsMatrix` for admin-portal modules) was reviewed and confirmed **intentional** design from a prior session — left untouched, not fixed.

**1. `CourseCarryOver.is_cleared` only updated by the progression batch, not live — fixed.** A student could already have a real, released passing grade for a carry-over course and still see it as open/locked on `my_courses`/`register_semester_course` until an admin next happened to run progression for them.
- [x] New `CourseCarryOver.sync_cleared_for_student(student)` classmethod (`eduweb/models.py`) — the single shared reconciliation, checking every open carry-over against a released passing `CourseGrade` and clearing it immediately if one exists. Returns the count cleared.
- [x] `management/progression.py apply_progression_decision` now calls this instead of hand-rolling the identical clearing loop a second time — removes a real duplication, not just adds a new call site.
- [x] Wired into every student-facing read path that checks `is_cleared` before using it: `_registrable_course_ids`, `dashboard`'s open-carry-overs widget, `my_courses`'s carry-over listing, and `register_semester_course`'s carry-over-lock check (all `student/views.py`).
- [x] Wired into the admin `carry_over_list` view (`management/views.py`) too — reconciles every distinct student with an open record before rendering, so an admin filtering to "Open" doesn't see a record already covered by a released grade.
- **Verified**: created a temporary released passing `CourseGrade` for a real open carry-over (`priya_escalate`/CSC102), confirmed `sync_cleared_for_student` cleared it immediately (`is_cleared=True`, `cleared_session` set to the grade's own session) without any progression run, then reverted both the grade and the carry-over row to their original state.

**2. Session activation vs. progression completion — no ordering enforced — guarded, not locked.** `AcademicSession.status` is manually controlled (nothing auto-closes it on dates) and `profile.year_of_study` only changes when progression is explicitly run — two independent admin actions. Activating a new session auto-closes the outgoing one (pre-existing behavior in `academic_session_set_current`) with no check that every student registered in it has actually been progressed first; an un-progressed student would then see their *old* level's courses inside what's meant to be their new session.
- [x] `management/views.py academic_session_set_current` now counts, for every session it's about to auto-close, students with a real `CourseRegistration` in it but no matching `ProgressionDecisionLog` — surfaces a `messages.warning()` naming the session and the count if any are found. A warning, not a block: a student under appeal or a still-pending manual override is a legitimate reason to proceed anyway.
- **Verified live** against real session data: activated the "upcoming" session (`2027/2028`), which correctly auto-closed the real current session (`2026/2027`) — confirmed via `django.contrib.messages.get_messages` that the exact warning fired ("'2026/2027' just closed with 7 student(s) registered in it who don't appear to have a progression decision for that session yet...", matching the real unprocessed count computed independently). Reverted both sessions' `status`/`is_current`/`registration_override` to their exact prior values afterward.

**3. Progression run vs. results actually released — no check — guarded, not locked.** `academic_progression`'s bulk run only required the session to be `closed`; nothing checked whether every grade in it had actually been released. Running it while some grades are still `pending`/`withheld` silently treats those courses as "not passed" (an unreleased pass doesn't count toward `core_passed`/`failed_courses`), which can wrongly repeat/fail a student whose real result — once released — would have cleared them.
- [x] Both the GET preview and the POST confirm branches of `academic_progression` (`management/views.py`) now count unreleased (`pending`/`withheld`) `CourseGrade` rows for the selected program/session and surface the count — a red warning banner on the preview page (`templates/management/academic_progression.html`, linking straight to Publish Results) and a `messages.warning()` on confirm. Not blocked: a deliberately withheld grade (e.g. a discipline hold) may be exactly why an admin is running progression anyway.
- **Verified live**: the real program/session combo with 22 pending/withheld grades correctly shows the warning banner on `/management/progression/?program_id=...&session_id=...`.

**`is_staff` bypass — reviewed, confirmed intentional, not touched.** Explicitly discussed with the user: `is_staff` unconditionally granting every `ADMIN_PORTAL_MODULES` module regardless of a restrictive `StaffPermissionsMatrix` row is being kept as designed — `is_staff` is treated as a high-trust Django-level flag allowed to override the app's own finer-grained restrictions, matching how a prior session's audit read the intent behind removing the old `is_staff == (role=='admin')` auto-sync.

**Housekeeping**: `femiadmin`'s 27 stale user-override `StaffPermissionsMatrix` rows (`can_view=False` on ~25 modules — a leftover test configuration from earlier this session, not a real intended restriction) were deleted, resetting her to the real role='admin' defaults. Verified via `eduweb.security_middleware._load_permissions` directly: she now has `can_view=True` on all ~20 core admin-portal modules that have seeded role-level rows.

**Verification performed**: `python manage.py check` and `makemigrations --check --dry-run` clean throughout. No test data or session-state mutations left behind — every session/grade/carry-over row touched during verification was reverted and reconfirmed against its original value.

---

### Phase 14 — real `can_view` enforcement wired into every GET view across `management`/`finance`/`instructor`/`support` (2026-07-22)

Closes the exact gap Phase 10 flagged and deliberately left as backlog: **`StaffPermissionsMatrix.can_view` — the "View" checkbox the permissions-modal UI presents as real, per-module control — was only enforced on GET for 2 of ~28 modules** (`academic_progression`, `results_publish`, both from Phase 9). Every other list/detail page across all four staff-portal apps was gated purely by the coarse role check (`is_admin`/`is_finance_manager`/`instructor_required`/`is_support_staff`); the matrix's `can_view` flag was checked nowhere for them, so a `can_view=False` row was decorative everywhere except those two views. Confirmed live at the start of this session using `femiadmin`'s now-reset role-default permissions as the baseline, then re-confirmed the gap was real by temporarily restoring a handful of her old restrictive rows one at a time and watching pages that should have redirected instead render full data — before wiring the fix.

**Method**: for every GET view identified as a list/detail page gated only by role, added a `can_view` check using each app's existing `_has_permission(request, module, action)` helper / `require_permission(module, action, redirect_to=..., skip_get=...)` decorator — never inventing a new pattern. Module keys were taken from a sibling create/edit/delete action on the same resource wherever one existed (e.g. `faculties_list` reuses `'academics'`, the exact key `faculty_create`/`faculty_edit` already checked for their own POST branches); where no sibling existed, the key was confirmed against `StaffPermissionsMatrix.MODULE_CHOICES` and the module's sidebar section in `templates/management/base.html`. Per the standing convention already established by `academic_progression`/`results_publish`, GET-only views got an inline `if not _has_permission(...): messages.error(...); return redirect(...)` at the top of the render path; views that mix a GET-render with a POST-mutation (already carrying a `can_create`/`can_edit`/`can_delete` check on the POST branch) got the identical inline check gated to `request.method == 'GET'` immediately before that POST branch, so neither check shadows the other. Dashboards (`management.dashboard`, `finance.finance_dashboard`, `instructor.dashboard`, `support.dashboard`) and shared inbox/notification pages were deliberately **not** touched — `StaffPermissionsMatrix.ROLE_DEFAULT_PERMISSIONS` itself comments out a `'dashboard'` key for every role, confirming this is existing intentional design (a portal's own landing page isn't a matrix module), not a gap to close.

**`management/views.py` — 47 GET views gained a new `can_view` check** (module key : view : line of the new check):
- `applications` — `applications_list:571`, `application_detail:625`
- `academics` — `faculties_list:1036`, `departments_list:3248`, `programs_list:3355`, `program_detail:3516`, `academic_sessions_list:3592` (GET branch, after the POST actions), `courses_list:4193` (GET branch), `intakes_list:4218`, `course_categories_list:4347`
- `blog` — `blog_posts_list:1109`, `blog_categories_list:1217`
- `user_management` — `users_list:1355` (GET branch, below the existing POST bulk-action dispatch), `role_assign:1717`, `user_quick_info` (decorator, `@require_permission('user_management','can_view', skip_get=False)` at line 1673), `user_permissions:1760` (GET/JSON branch, mirrors the existing POST `can_edit` check at 1765)
- `lms_courses` — `lms_courses_list:2227`, `lms_academic_course_data` (decorator at 2290), `lms_course_detail` (decorator at 2409)
- `security_audit` — `audit_logs_list:2567`, `audit_log_detail:2620`, `security_dashboard:2685`
- `communications` — `broadcast_center:2724`, `broadcast_edit:2826` (GET branch), `contact_messages_list:4454`, `contact_message_detail:4497`, `announcements_list:4569`
- `academic_progression` — `carry_over_list:3931` (the existing `academic_progression`/`results_publish` GET checks at 3856/4015/4113 predate this session — Phase 9 — and were not touched)
- `enrollments` — `enrollments_list:4690`, `certificates_list:4836`, `reviews_list:4930`, `badges_list:5035`, `student_badges_list:5124`
- `finance` — `transactions_list:5330`, `transaction_detail:5402`, `financial_analytics:5443`
- `finance_payroll` — `staff_payroll_list:5615`
- `site_content` — `site_config_general:5757`, `site_config_index:5783`, `site_config_about:5809` (all three GET-branch-only, since these are edit-only single-object pages), `site_milestones_list:5834`, `testimonials_list:5908`, `institution_members_list:5980`
- `library` — `library_items_list:6066`, `library_item_edit:6157` (GET branch)
- `exams` — `admin_exam_list:6237`, `admin_exam_detail:6297`, `admin_question_moderation:6453` (GET branch), `admin_exam_timetable_update:6468` (GET branch), `admin_exam_responses:6528`
- `intake_edit`'s GET/JSON modal-prefill branch (line 4302) also gained a `can_view` check (`'academics'`), returning a JSON 403 rather than a redirect since it's an AJAX-only endpoint.
- **Deliberately left unchanged, not an oversight**: `system_config_list/create/edit/delete`, `branding_config`, `email_config`, `notification_config`, `payment_gateways_list/create/edit/delete`, `subscription_list` (finance app) — all gated by `is_superuser_only()`/a bare `_is_superuser` check that already excludes every matrix-granted staff account, a stricter and deliberate lock (per Phase 8's finding on `is_superuser_only()`) that a `can_view` check would only ever be a no-op against (superusers already bypass `_has_permission` unconditionally). `management.dashboard`, `admin_inbox`, `admin_compose_message`, `admin_message_thread`, `notifications_view` also left alone — none of these are `StaffPermissionsMatrix` modules (confirmed against `MODULE_CHOICES`; `admin_inbox`/`notifications_view` are shared by every internal role via `is_staff_member`, not module-gated by design, matching the existing `dashboard` precedent).

**`instructor/views.py` — 45 GET views gained a new `can_view` check**, since every one of the ~30 existing `require_permission` decorators in this file used the default `skip_get=True` — meaning GET had **never** been matrix-gated anywhere in this app before this session, not even partially (unlike `management`/`support`, which each had at least the two/many pre-existing exceptions):
- `instructor_courses` — `course_list:168`, `course_manage:213`, `course_edit:376` (GET branch), `course_objectives:425` (GET branch), `lesson_list:565`, `lesson_create:599` (GET branch), `lesson_edit:648` (GET branch), `students_list:1286`, `student_progress:1310`
- `instructor_assessments` — `quiz_list:734`, `quiz_create:765` (GET branch), `quiz_edit:821` (GET branch), `quiz_questions:867`, `question_create:905` (GET branch), `question_answers:963` (GET branch), `assignment_list:1022`, `assignment_create:1051` (GET branch), `assignment_edit:1107` (GET branch), `assignment_submissions:1156`, `grade_submission:1194` (GET branch), `all_quizzes:1466`, `all_assignments:1488`, `quiz_results:2027`, `quiz_attempt_detail:2068` (GET branch), `ajax_course_details:2569` (JSON 403), `create_assessment:2633` (GET branch), `ajax_course_lessons:2866` (JSON 403), `exam_list:2889`, `exam_detail:2935`, `exam_question_create:3132` (GET branch), `exam_question_edit:3163` (GET branch), `exam_grade_response:3469` (GET branch)
- `instructor_analytics` — `course_statistics:1570`, `student_analytics_progress:1664`, `reviews_ratings:1735`
- `instructor_resources` — `resources:1832`
- `instructor_communications` — `announcement_create:1413` (GET branch), `announcement_list:1952`, `announcement_edit:1974` (GET branch), `messages_inbox:2166`, `messages_sent:2187`, `message_thread:2206`, `message_compose:2287` (GET branch), `discussions:2392`, `discussion_detail:2411`
- `instructor.dashboard` deliberately left alone (see the dashboard-exemption note above).

**`support/views.py` — already the most thoroughly gated of the four apps going into this session** (commit `0799e63` had already added `@require_permission(module, 'can_view', skip_get=False)` to essentially every list/detail view — `ticket_list`, `ticket_detail`, `kb_list`, `kb_article_detail`, `faq_list`, `canned_list`, `sla_list`, `department_list`, `agent_list`, `analytics`, `chat_list`, `announcement_list`, `audit_log` all confirmed already correctly gated). Only two genuine gaps found and closed:
- `support_knowledge_base` — `kb_article_edit:757` (GET branch; the article-detail view was already gated, but this separate edit-form URL bypassed it).
- `support_communications` — `api_canned_response:1266` (AJAX GET/POST endpoint returning a canned response's full body/title; JSON 403 on denial).
- `support.dashboard`/`api_ticket_stats` deliberately left alone — the former per the dashboard-exemption note above, the latter returns only aggregate counts (no per-record data) and has no confident single-module mapping; flagged rather than guessed.

**`finance/views.py` — 0 new checks; both remaining unguarded views confirmed intentional, not gaps**:
- `finance_dashboard` — left alone, matching the dashboard-exemption note above (added an explanatory comment in the code so a future pass doesn't mistake the omission for one).
- `subscription_list` — gated by a bare `@user_passes_test(_is_superuser)`, already stricter than any matrix `can_view` check could make it (a matrix-granted `finance_subscriptions.can_view=True` role user still can't get past the outer superuser-only gate) — same class as `management`'s `is_superuser_only()` exemption, left untouched.
- Every other finance view (`payroll_management`, `payroll_detail`) already had a real `can_view` check from Phase 4 — re-confirmed intact, not re-added.

**No new migration required.** Unlike the `finance_payroll`/`finance_payments`/`results_publish` gaps closed in migrations `0044`/`0048`/`0053`, live testing this session found every role that needed a `can_view=True` default for a module this pass wired up already had one (verified via the full `femiadmin`/`super`/`financial`/`instructor`/`support` sweep below — no unexpected redirect fired for an account that should have had access).

**Verified — full sweep, `django.test.Client` + `override_settings(ALLOWED_HOSTS=['testserver'])` against the real dev DB**:
- **`femiadmin`** (real non-superuser admin, reset to bare role='admin' defaults, 0 override rows): all 32 changed `management:` list/detail URLs return `200` with an empty redirect chain — `/management/applications/`, `/faculties/`, `/blog/posts/`, `/blog/categories/`, `/users/`, `/users/role-assign/`, `/lms-courses/`, `/audit-logs/`, `/security/`, `/broadcast/`, `/departments/`, `/programs/`, `/academic-sessions/`, `/progression/carry-overs/`, `/courses/`, `/intakes/`, `/categories/`, `/contact-messages/`, `/announcements/`, `/enrollments/`, `/certificates/`, `/reviews/`, `/badges/`, `/student-badges/`, `/transactions/`, `/analytics/financial/`, `/payroll/`, `/site-config/milestones/`, `/site-config/testimonials/`, `/site-config/members/`, `/library/`, `/exams/` — plus spot-checked detail pages keyed by a real pk/slug (`application_detail`, `program_detail`, `transaction_detail`, `admin_exam_detail`) all `200`.
- **`super`** (real superuser): same 10-URL subset re-checked, all `200`, confirming the superuser bypass is completely unaffected by any of this session's changes.
- **`financial`** (real `role='finance'` account, existing per-user override rows): `/finance/payroll/` → `200`; `/finance/subscriptions/` correctly redirects (pre-existing `_is_superuser` gate, unrelated to this session).
- **`instructor`** (real `role='instructor'` account): `/instructor/`, `/instructor/courses/`, `/instructor/assessments/quizzes/`, `/instructor/assessments/assignments/`, `/instructor/analytics/courses/`, `/instructor/analytics/students/`, `/instructor/analytics/reviews/`, `/instructor/resources/`, `/instructor/messages/`, `/instructor/messages/sent/`, `/instructor/exams/` all `200`; spot-checked `course_manage`, `course_edit`, `lesson_list`, `quiz_questions` (real owned course/lesson/quiz), and `students_list` against her one real assigned course, all `200`.
- **`support`** (real `role='support'` account): `/support/`, `/support/tickets/`, `/support/kb/`, `/support/faqs/`, `/support/canned/`, `/support/analytics/`, `/support/chats/`, `/support/announcements/` all `200`.
- **Restriction tests — 6 modules across all 4 apps**, each via a temporary flip of a real matrix row's `can_view` to `False` (existing user-override rows for `financial`/`instructor`/`support`; `femiadmin` had none, so a temporary row was created and then deleted rather than left at `False`), confirmed the page now redirects with the correct "You do not have permission to view ..." message rendered on the landing page, then reverted and reconfirmed the exact original value/absence:
  - `femiadmin` / `academics` → `/management/departments/`: redirected to `/management/`, denial message rendered.
  - `femiadmin` / `security_audit` → `/management/audit-logs/`: redirected to `/management/`, denial message rendered.
  - `femiadmin` / `enrollments` → `/management/enrollments/`: redirected to `/management/`, denial message rendered.
  - `financial` / `finance_payroll` → `/finance/payroll/`: redirected to `/finance/`, denial message rendered.
  - `instructor` / `instructor_assessments` → `/instructor/assessments/quizzes/`: redirected to `/instructor/`, denial message rendered.
  - `support` / `support_knowledge_base` → `/support/kb/contacting-your-instructor/edit/`: redirected through `/support/kb/` (itself gated on the same module) to `/support/`, denial message rendered.
- **No leftover mutations**: `StaffPermissionsMatrix.objects.count()` confirmed `130` both before and after the entire verification pass (identical to the pre-session baseline); `femiadmin`'s override-row count confirmed `0` both before and after. All temporary verification scripts (`_perm_verify.py`, `_perm_verify2.py`, written to the repo root to drive the Django test Client sweep) deleted after use.

**`python manage.py check` and `python manage.py makemigrations --check --dry-run`** run clean after every batch of edits (management, then instructor, then finance/support) and once more at the very end.

**Independent re-verification of this phase (orchestrating session, not the agent) found and fixed one genuine pre-existing bug, unrelated to this session's work.** A full regression sweep across all 4 apps × 5 real accounts (`super`, `femiadmin`, `financial`, `geokol`/instructor, `support`) flagged one hard crash: `GET /instructor/messages/compose/` — `NameError: name 'MessageForm' is not defined`. `MessageForm` genuinely exists in `instructor/forms.py:772`; it was simply never added to `instructor/views.py`'s `from .forms import (...)` block. Confirmed via `git show HEAD` that this predates every change in this entire session (Phases 1–14) — this view has been a guaranteed crash for any instructor who has ever reached it, entirely independent of the `can_view` gate just added in front of it. Fixed with a one-line import addition; re-ran the full sweep afterward with zero remaining flagged crashes.

---

### Phase 15 — `management` re-audit: student↔management sync verified, dead code removed, endpoint sweep (2026-07-22)

Prompted directly by the user, following Phase 12–14's `student` closure: (1) confirm the `student`-app fixes actually hold hands with `management` rather than just being correct in isolation, and (2) re-audit `management/views.py` end-to-end for dead code and endpoint integrity while the file was already fresh in context from Phase 14.

**Student↔management sync — verified live, not just re-read from prior phases.** The shared contract between the two apps is `CourseGrade.result_status` (`management.results_publish_detail`'s POST branch flips it; `student.grades()`'s `academic_grades` query is supposed to honor it). Reproduced the exact mechanism end-to-end: took a real released `CourseGrade` row (`talia`/CSC104, pk 1), flipped it to `withheld` (identical `.update(result_status=...)` call `results_publish_detail` itself issues), and inspected `grades()`'s actual template context (`response.context['academic_grades']`, not a substring scrape of the HTML — a first pass using substring-matching on the course code gave a false "still visible" result because `talia` has a *second*, unrelated `CourseGrade` row for the same course code in a different session, which legitimately stayed visible; re-ran against the specific row's pk once that was caught). Confirmed: pk 1 present in `academic_grades` while `released`, absent while `withheld`, `pending_results_count` incremented correctly, and re-appeared correctly after reverting. This is the same mechanism Phase 9 built and Phase 10–13 re-verified in other contexts; this pass adds a fresh, targeted live re-confirmation specifically of the management-write → student-read round trip, with zero drift found.

**Dead code removed from `management/views.py`:**
- [x] **`send_application_submission_email`** (was ~95 lines) — confirmed via repo-wide grep to be called from **nowhere**, not even the one place its name suggests (application submission is real students hitting `eduweb.apply`/`eduweb.emailservices.send_application_confirmation_email`, a completely different, working function). Also confirmed genuinely broken, not just unused: it referenced `application.course.name`/`.course.get_degree_level_display()`, but `CourseApplication` has no `course` field (only `program`) — this function would have raised `AttributeError` on its first real invocation had anything ever called it. Deleted outright.
- [x] **Dead unreachable tail after `send_department_approval_email`** — a bare tuple-expression statement (`Department, Program, AcademicSession, CourseIntake, CourseCategory, SupportTicket, TicketReply, ContactMessage, Announcement, Faculty`, evaluated and discarded, leftover from some earlier refactor of an import block) plus an orphaned comment header ("Helper: restrict to admin/staff") with no code beneath it, both sitting after the function's own `try/except` (both branches already `return`). Removed.
- [x] **One stale `StaffPermissionsMatrix` row** (`role='admin', module='support'`, id 76) — `'support'` predates the `support_tickets`/`support_knowledge_base`/`support_communications`/`support_analytics`/`support_config` split (confirmed absent from the current `MODULE_CHOICES`) and confirmed via repo-wide grep that no `_has_permission(request, 'support', ...)` call exists anywhere. Deleted directly (not a migration matter — this is leftover row data, not seeded schema state); total row count went from 130 → 129.
- [x] **Unused imports** — `uuid`, `Cast`, `CharField`, `F` (all from `django.db.models`/stdlib), `SupportTicket`, `TicketReply` (from `eduweb.models`, orphaned since the old in-app ticket console was removed per Phase 3's own note), and `send_certificate_ready_email` (from `eduweb.emailservices`) — all confirmed zero-reference via a repo-scoped `\bname\b` grep excluding the import line itself. The last one specifically checked for a *missing wire-up* rather than assumed dead: `Certificate.mark_paid()` (the real trigger point, per the email helper's own docstring) is correctly called from `eduweb/views.py:2108` inside the real Stripe fee-payment confirmation flow, which already calls `send_certificate_ready_email` itself — the `management/views.py` import was simply a stale copy-paste, not a missing notification.

**Endpoint sweep — every `management:` URL name cross-referenced against every template.** 149 URL names total; 9 initially came back with zero `{% url 'management:X' %}` hits. Investigated each individually rather than assuming any were dead:
- `badge_edit`, `certificate_edit`, `review_edit`, `faculty_delete`, `lms_academic_course_data`, `lms_course_detail`, `lms_course_delete` — **false positives**, all genuinely reachable via hardcoded JS string concatenation (`EDIT_BASE + slug + '/edit/'`, `fetch(\`/management/lms-courses/${pk}/...\`)`) rather than the Django `{% url %}` tag, so a pure-template grep missed them. Confirmed live in each template's own `<script>` block.
- `required_payments_list`, `tickets_list` — confirmed intentional, documented legacy-bookmark redirect stubs (Phase 5/3 respectively), deliberately not linked from anywhere new.
- **One real, fixed gap**: `enrollment_edit`/`enrollment_delete` (already fully built — validation, notifications, dependent-record guard, `can_edit`/`can_delete` matrix checks — see Phase 3's own note flagging this as unreachable) were confirmed still completely unreachable — `enrollments_list.html` had no Actions column and no link anywhere else in the repo. Added an Actions column (Edit link + Delete confirm-form, matching the existing pattern used by `badges.html`) to `templates/management/enrollments_list.html`; no view or URL changes needed since both endpoints were already fully built. **Verified live** as `femiadmin`: the list page now renders both real per-row URLs, and `GET /management/enrollments/<pk>/edit/` returns `200`.
- **One real, fixed dead-link bug found while investigating the LMS-course false positives**: `templates/management/lms_course/list.html`'s edit-modal `fetch(...).catch()` fallback linked to `/management/lms-courses/<pk>/edit/` — a route that has never existed (`lms_course_detail`'s own docstring confirms "no standalone detail page needed", edit is modal-only). This fetch failure path is now *more* reachable than before Phase 14, since `lms_course_detail` gained a real `can_view` check that responds `403` on denial, which is exactly the kind of response this `.catch()` handler exists for — so a matrix-restricted user hitting "Edit" would previously have landed on a 404 dead end. Fixed to a plain retry message instead of a broken link.

**Other confirmed-healthy findings from this pass (no action needed):**
- No large commented-out code blocks remain anywhere in `management/views.py` (Phase 3 already swept these).
- Every `def` in the file is either a real routed view (`views.X` in `urls.py`), a decorator-referenced predicate (`is_admin`, `is_staff_member`, `is_superuser_only`, passed bare to `@user_passes_test(...)`/`redirect_to=...`), or an internal helper called elsewhere in the same file — no further orphaned functions found beyond the two removed above.
- `management/forms.py` and `management/progression.py` import blocks in `views.py` — zero unused names.

**Verification performed**: `python manage.py check` and `python manage.py makemigrations --check --dry-run` clean after every batch (dead-code removal, import cleanup, template fix) and once more at the end. The `CourseGrade` sync test reverted the one row it touched and re-confirmed its exact original `result_status` afterward; the stale `StaffPermissionsMatrix` row deletion is the only non-reverted data change in this phase, and is a genuine one-off cleanup of confirmed-orphaned data, not a schema/seed change requiring a migration. Temporary verification scripts (`_sync_verify.py`, `_sync_verify2.py`, `_sync_verify3.py`) written to the repo root, deleted after use.

---

## eduweb

**Fixed since last pass**
- `get_payment_summary` now correctly uses `settings.STRIPE_PUBLIC_KEY` (was previously also broken here).

**Still broken (as of 2026-07-13; all four resolved in Phase 5/6, 2026-07-14 — see above)**
- ~~`get_student_fee_summary` — both guaranteed crashes remain~~ — fixed in Phase 6 (`select_related('program__department__faculty')`, `settings.STRIPE_PUBLIC_KEY`, `fee.currency`). This was actually a *second* occurrence of the exact bug already fixed once in Phase 1 for a sibling function — worth noting the pattern (bad `select_related` on `@property` fields, wrong Stripe key name) had been copy-pasted.
- ~~`SignUpForm.clean_email` still defined twice~~ — fixed in Phase 5.
- ~~Email-verification token still never expires~~ — fixed in Phase 6 (7-day expiry, migration `0049`).
- Currency hardcoding is still real but only partly addressed: the `'gbp'`/`'usd'` Stripe mismatch in `create_payment_intent` was fixed in Phase 6 (both now `'usd'`), but there's still no single source of truth (`AllRequiredPayments.currency`/`SiteConfig`) driving what gets charged — a bigger build, still open.

**New findings**
- `DigitalCampus/settings.py:24,26` — `SECRET_KEY` and `DEBUG` both fail open to insecure defaults if `.env` is missing/misconfigured, rather than failing to start.
- `BlogPost.increment_views` (`models.py:918-921`) does a plain read-modify-write (`self.views_count += 1; self.save()`) instead of `F('views_count') + 1` — undercounts under concurrent traffic. Notably inconsistent: the `library` view/download counters right next to it in the same file do use `F()` correctly.
- ~~Stripe webhook (`views.py:2130-2145`) remains solid~~ — **correction, Phase 6**: this claim was never actually true. The webhook's signature verification is genuinely solid, but (a) a view function literally named `settings` elsewhere in the same module shadowed `django.conf.settings` for the whole file, breaking `settings.STRIPE_WEBHOOK_SECRET` here, and (b) the application-payment branch was permanently unreachable dead code (the metadata key it checks for was never set by the code that creates the PaymentIntent). Both fixed in Phase 6.
- No raw SQL, `eval`/`exec`/`pickle.loads`, or additional `csrf_exempt` usage found anywhere in the repo.

---

## management

**New: end-of-session progression/carry-over (`management/progression.py`, wired at `views.py:3339-3439`)**

The feature the last audit's cross-cutting theme #1 asked for now exists, and the CGPA math itself is correct — `compute_cgpa` properly weights grade points by `credit_units`. But four issues need fixing before it should be trusted for real academic decisions:

- **Inherits the `CourseGrade` unreliability** described in cross-cutting theme #2 above — exam-only courses never get a `CourseGrade` row, so `core_passed` is permanently `False` for them.
- **No idempotency guard.** `apply_progression_decision` never checks for an existing `ProgressionDecisionLog` before mutating; the eligibility filter (`progression_status__in=['active','repeated','probation']`) still includes a student immediately after they're promoted, so a resubmitted POST (double-click, back-button, retry) can advance them twice. Client-side `confirm()` dialog only — no server-side dedup.
- **No session-state check.** Every `AcademicSession` is offered for progression, including ones that are `'upcoming'` or still `'active'` — nothing restricts the action to a `'closed'` session.
- **Escalation-avoidance loophole.** `failed_courses` only counts courses the student is registered for *in the current session*. A student who simply doesn't re-register for a previously-failed core course keeps `core_passed=False` (blocked from promotion) but never shows up in `failed_courses`, so `new_status` stays `'repeated'` forever and never escalates to `'probation'`/`'withdrawn'` no matter how many sessions pass.
- **No `transaction.atomic()`** around the per-student batch loop (`views.py:3376-3379`) — a mid-batch exception leaves an inconsistent mix of fully-applied, partially-applied, and untouched students with no rollback.
- Permission gating is inconsistent even within this one feature: GET has no `can_view` check at all (gated only by the coarse hand-rolled `is_admin`); the two POST mutation branches do consult `StaffPermissionsMatrix` (`can_approve`/`can_edit`) — the only two `StaffPermissionsMatrix` reads that exist anywhere in `management/views.py`.
- Student-side gap: course-registration queries (`student/views.py:482-492,703-710`) filter by `year_of_study` only, with no exclusion for courses the student already has a passing grade for — a repeating student can see and re-register for courses at that level they already passed, since only *failed* courses get a `CourseCarryOver` row.

**Still broken (re-verified)**
- `financial_analytics` (`views.py:4598`) — still zero auth decorators, reachable by an unauthenticated visitor.
- `StaffPermissionsMatrix` still functionally decorative — only the two progression POST branches read it; every other view still gates via `is_admin`/`_is_admin`/`_is_staff`, none importing `eduweb.decorators.admin_required`.
- Delete cascades (`faculty_delete:888`, `course_delete:1027`, `department_delete:3155`, `program_delete:3244`) still bare `.delete()` with no dependent-count confirmation.
- `issue_transcript` (`views.py:608-631`) still just flips a boolean and sends an email — no document is generated.
- The role-assign fix (92a2d97) is **cosmetic, not a closed hole**: it only excludes `role='student'` from the dropdown in `role_assign` (`views.py:1600`). The actual mutating endpoints, `user_change_role` (`views.py:1512`) and `user_permissions` (`views.py:1633`), still accept any `pk` with no role exclusion — reachable only by an already-`_is_admin`-gated user, so not a privilege-escalation path, but the fix doesn't prevent an admin from directly POSTing a student into a staff role outside the picker UI.

---

## instructor

**Still broken (re-verified, both are the same as last pass, neither touched)**
- Exam-question file-import still constructs `ExamQuestion.objects.create()` with removed fields (`difficulty`, `order`, `import_row_number`, confirmed removed in `eduweb/models.py:5465-5646`) at `instructor/views.py:3032-3037` (xlsx) and `3052-3057` (docx). The standalone import view still catches this (`views.py:2833-2840`, fails gracefully). The inline call inside `create_assessment` (`views.py:2441`) still has **no try/except** — uploading a question file while creating a new exam still 500s.
- `grade_submission` (`views.py:1074-1082`) still writes `request.POST.get('score')` straight to `.save()` with no `full_clean()` — negative/out-of-range/non-numeric scores still get through.
- `course_create`/`course_delete` still disabled (redirect to "contact an admin" / unconditional `PermissionDenied`); "send welcome email" checkbox still a no-op TODO (`views.py:1198-1200`).

**Confirmed healthy:** ~85 views spot-checked, all still carry `@login_required` + `@instructor_required` with correct `instructor=request.user` ownership scoping. No new gaps found. Stale FAQ/quick-links content from the last audit is gone — that section of the file was refactored away, not fixed in place.

---

## student

**Still broken (re-verified)**
- The three-way grade disagreement is unchanged: dashboard reads real `CourseGrade`; "Grades & Performance" still has its query commented out; "Academic Records" still builds a third proxy from `StudentExamResponse` directly. See cross-cutting theme #2 for why this now matters beyond cosmetics.
- Exam endpoints still under-gated: `exam_instructions` (3818), `start_exam` (3863), `get_exam_data` (3917) carry only `@login_required` — no student-role check, no re-verification that the requester is enrolled in that exam's course. Exam slugs are plain `slugify()`, not random tokens, so a logged-in user who derives/guesses a slug can create a response row and pull exam data for a course they're not enrolled in.
- No fee gate on `enroll_course` (comment "All LMS courses are free" still at `views.py:1323`); prerequisites still display-only, never enforced at registration; N+1 patterns in `course_detail` (per-lesson queries in a loop) and the 28-day activity heatmap (per-day queries) both unchanged.

**Fixed since last pass**
- Support tickets now actually reach staff: `submit_ticket` (student-facing, routed through the new `support` app) creates a real `SupportTicket` row via `StudentSupportTicketForm` — the old plain-`forms.Form`, email-only dead end is gone.

**New finding:** `my_courses.html` correctly flags carry-over courses to the student (orange row, "failed Nx previously") — the display side is good. The gap is upstream, in the registration query noted under `management` above (passed courses at the repeated level aren't excluded from what's offered).

---

## finance

**The "gated by per-module permission" commit (363585b) is template-only.** `finance/views.py` never imports or checks `StaffPermissionsMatrix` — every view is still gated solely by the local `is_finance_manager()` (`views.py:27-33`, unchanged, still not using `eduweb.decorators.finance_required`). The matrix flags (`can_edit`/`can_delete`/`can_export`) only control whether a button renders in the template. A finance user can still POST directly to any finance action (e.g. `payroll_delete`) regardless of their matrix row for that module.

Everything else from the last pass (payroll CRUD, dashboard KPIs, subscription list as display-only, the `except (ImportError, Exception): pass` redundancy) is unchanged — not re-litigated here.

---

## payment

**Fixed since last pass**
- The duplicate `refund_payment` definition and duplicate URL registration are both gone — one function, one route.
- Refund ordering is correct and was already: Stripe `stripe.Refund.create` (`views.py:283-286`) runs and is confirmed successful before any local `payment.save()` (`views.py:306`); a Stripe error aborts cleanly with no local state change.

**Still broken**
- Refund still does **not** revert `CourseApplication` payment state — only `ApplicationPayment.status`/`.failure_reason` are updated (`views.py:298-306`); `CourseApplication.payment_status` (which `is_paid` reads, `models.py:1998-2011`) is never touched. A refunded application still reports as fully paid.
- **The commit's own claim of "gated by `finance_payments.can_edit`" is false at the enforcement layer.** `refund_payment` is gated only by `@user_passes_test(is_finance_manager)` (`views.py:20-26,240-241`) — i.e., `role == 'finance'`, full stop. `StaffPermissionsMatrix`/`finance_payments.can_edit` is referenced only in `templates/finance/payment_detail.html` to hide the refund button. Any finance-role user can POST to the refund endpoint and process a real Stripe refund regardless of their matrix setting.
- `required_payments_list` (`views.py:156-157`) is still a two-line stub with no query.

---

## library

No changes since the last pass (last commit to this app predates 2026-07-08). Access control is still binary `public`/`members`, no program/department/course-level granularity. Still the cleanest app of the eight.

---

## support (new — first audit)

Not mentioned in `CLAUDE.md`'s app map; wired into `DigitalCampus/urls.py` under `/support/`. Worth adding to `CLAUDE.md`'s app-map table.

A genuinely solid helpdesk console: tickets (list/detail/create/reply/assign/escalate/reopen) with SLA policies, departments, agents (with availability/load tracking), a Markdown knowledge base with voting, FAQs, canned responses, an announcements system, and an immutable `SupportAuditLog`. A `ChatSession`/`ChatMessage` model exists but has no view wired up beyond a bare list.

- **Same cosmetic-gating problem as finance.** The e128fed commit only touched templates. `support/views.py` gates purely via `support_required`/`support_admin_required` (`support/permissions.py:36-59`), which check `role in ('admin','support')`/`is_staff` — nothing module-specific. A support user with `can_edit=False` on the knowledge-base module can still POST to `kb_article_edit` and it succeeds.
- **No IDOR found.** The self-service reply path correctly scopes by owner: `get_object_or_404(SupportTicket, ticket_id=..., user=request.user)` (`views.py:83`) — a student can't reply to someone else's ticket. Staff-console visibility (any agent sees any ticket) is by design, not a bug.
- No raw SQL, no `|safe` on user-submitted ticket/reply content (uses `linebreaks`/`linebreaksbr`, which still escape).
- Minor: several staff-side create actions (`sla_save`, `department_save`, `announcement_create`) skip `ModelForm` validation in favor of `.strip()` checks — a data-quality gap, not a security one.

---

## Frontend-wide template sweep (new this pass)

- **CSRF:** every template with `method="post"` (95 top-level, 12 under `support/`) also contains `{% csrf_token %}`. No gaps found.
- **`|safe` usage:** every hit reviewed traces back to admin/staff-authored content (`SiteConfig` embeds, blog post bodies, lesson content, icon/JSON strings) or server-side `json.dumps()` feeding a chart script — none renders arbitrary end-user input. The one point worth a policy decision: `lesson.video_url`/`lesson.content` (`templates/students/lesson.html:44,90`) are instructor-authored, a broader trust boundary than pure-admin content — if instructor accounts are ever considered semi-trusted, this is stored-XSS-capable.
- **One JS-string interpolation inconsistency:** `templates/applications/admission_letter.html:462` interpolates `{{ application.application_id }}` into a JS string literal without `|escapejs`, while the line directly above it does use `|escapejs`. Low risk (the value is a server-generated ID, not free text) but worth fixing for consistency.

---

## Recommended next steps

*Updated 2026-07-14 — items completed in Phases 1–3 above are checked off in place; nothing below was re-numbered so old references still resolve.*

### Quick wins — same file, isolated, still undone from last pass
1. [x] `eduweb`: fix `get_student_fee_summary`'s two guaranteed crashes — done, see Phase 1 (`select_related('program__department__faculty')`, `settings.STRIPE_PUBLIC_KEY`, `fee.currency`).
2. [x] `eduweb`: delete the dead first `SignUpForm.clean_email` — done, see Phase 5.
3. [x] `instructor`: wrap the inline exam-import call in `create_assessment` in the same try/except the standalone view already has — done, see Phase 2 (`_parse_exam_questions_from_file` shared helper).
4. [x] `instructor`: route `grade_submission`'s score through validation — done, see Phase 2 (range-checked `Decimal` parse).
5. [x] `management`: add a real decorator to `financial_analytics` — confirmed already in place (`@login_required` + `@user_passes_test(is_admin)`); this finding was stale by the time Phase 3 re-checked it, not actually fixed by this session's work.
6. [x] `payment`: revert `CourseApplication.payment_status` inside `refund_payment`; make `required_payments_list` query `AllRequiredPayments` — done, see Phase 4.
7. [x] `eduweb`: fix `SECRET_KEY`/`DEBUG` to fail closed — done, see Phase 5.
8. [x] `eduweb`: switch `BlogPost.increment_views` to `F('views_count') + 1` — done, see Phase 5.

### The one thing to fix before trusting any "permission-gated" claim
Wire `StaffPermissionsMatrix` checks into the actual view/decorator layer in `finance`, `payment`, and `support` — not just templates. Right now three separate commits describe themselves as adding permission gating, and none of the three enforce anything server-side. This is worse than having no gating claim at all, because it reads as done. **Closed for all four apps now** — `management` in Phase 3, `finance`/`payment`/`support` in Phase 4.

### Before the progression feature is used for real decisions
1. [x] Feed `StudentExamResponse` into `CourseGrade` — the three-way grade-source disagreement itself was unified (Phase 1) and the additional `quiz_submit` recompute gap closed (Phase 1 addendum); the remaining backfill (pre-existing rows created under the old exam-blind logic) is done too, see Phase 5 (`backfill_course_grades` management command).
2. [x] Add a `ProgressionDecisionLog` existence check before `apply_progression_decision` mutates (idempotency), restrict the session picker to `status='closed'`, wrap the batch loop in `transaction.atomic()`, and change `failed_courses` to look across all sessions a core course was attempted in. **Discovered 2026-07-14 (Phase 5) already done** — commit `5916ef0` ("academic progress") had already added all four (`already_processed_student_ids()` in `management/progression.py`, the `session.status != 'closed'` guard, the outer `transaction.atomic()` around the batch loop, and `compute_progression_decision`'s `failed_courses` now checking open `CourseCarryOver` rows regardless of current-session registration), this tracker section just wasn't refreshed to reflect it. Re-verified end-to-end via test client against real dev DB: an open-session POST is rejected with the student left unchanged, and resubmitting an identical confirm POST against a closed session creates exactly one `ProgressionDecisionLog` row (not two).
3. [x] Exclude already-passed courses from the semester registration listing for repeating students — done, see Phase 1 (`my_courses`) and Phase 1 addendum (`_registrable_course_ids` closing the deeper credit-cap scoping bug this same requirement depended on).

### Bigger builds still open from last pass
1. [x] Unify the three grade sources — see item 1 above, fully done including backfill (Phase 5).
2. [x] Make required payments actually gate registration/enrollment — done, see Phase 5. Scoped by product decision to new course registration only (`register_semester_course`), not portal access or already-registered courses.
3. [x] Consolidate role-gating decorators — done, see Phase 5, but narrower than originally framed: the only actual byte-for-byte duplicate was `is_finance_manager` (`finance`+`payment`), now shared via `eduweb.decorators`. `management`'s and `support`'s role predicates were each already single-copy with deliberately different semantics from the `eduweb.decorators` equivalents (e.g. `is_staff`-alone bypass) — left alone rather than risk a real access-control change by forcing uniformity.
4. [x] Gate `student` exam endpoints by role + enrollment — done, see Phase 1 (`exam_instructions`/`start_exam`/`get_exam_data` now `@student_required` + enrollment-scoped).
5. [x] Give instructors real course create/delete, or remove the dead UI that implies they can — done, see Phase 5 (removed; decided against building it out).
