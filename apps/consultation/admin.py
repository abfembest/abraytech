# No Django admin registration here on purpose — consultation slots, topic
# pricing, and bookings are managed from the branded staff portal instead
# (see apps/management/views.py's consultation_* views, under the
# "Consultation" sidebar entry), not through /admin/.
